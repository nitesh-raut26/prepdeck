---
title: "CAP, Consistency & ACID vs BASE"
summary: "What CAP actually forces you to choose during a partition, the spectrum of consistency models, and when strong consistency is worth its latency cost."
order: 5
tags: ["CAP", "consistency", "distributed-systems"]
---

## CAP, stated correctly

A distributed store can't simultaneously guarantee all three of **C**onsistency,
**A**vailability, **P**artition-tolerance. Since network partitions *will* happen,
P isn't optional — so the real choice **during a partition** is:

- **CP** — refuse some requests to stay consistent (e.g. a banking ledger).
- **AP** — keep serving, accept that replicas may diverge and reconcile later
  (e.g. a social feed, a shopping cart).

Here's the moment CAP actually bites — the same partition, the two choices:

<Mermaid chart={`flowchart TB
  subgraph healthy["Network healthy — no choice needed"]
    A1[(Replica A)] <-->|replicating| B1[(Replica B)]
  end
  subgraph partition["⚡ Partition — A and B can't talk. A write arrives at A."]
    direction TB
    A2[(Replica A — has new write)] x--x|link down| B2[(Replica B — stale)]
  end
  partition --> CP["CP: B refuses reads/writes<br/>until the link heals.<br/>Consistent, but unavailable.<br/>→ ledgers, inventory"]
  partition --> AP["AP: B keeps answering<br/>with stale data; reconcile later.<br/>Available, but divergent.<br/>→ feeds, carts, counters"]
  style CP fill:#E0E7FF,stroke:#4F46E5
  style AP fill:#DCFCE7,stroke:#16A34A`} />



<Callout type="warn" title="CAP is a partition-time choice, not an always-on label">
When the network is healthy you can have both consistency and availability. CAP
only bites *during* a partition. PACELC extends it: **E**lse (no partition),
you still trade **L**atency vs **C**onsistency — strong consistency costs round
trips.
</Callout>

## The consistency spectrum

| Model | Guarantee | Example |
|---|---|---|
| Strong | every read sees the latest write | balances, inventory decrement |
| Read-your-writes | you see your own updates immediately | edit your profile |
| Monotonic reads | you never see time go backwards | a paginated feed |
| Eventual | replicas converge "soon" | view counts, social timelines |

Most large systems are **eventually consistent** by default and reserve strong
consistency for the few operations that truly need it.

Replication lag is where this spectrum stops being abstract. A write lands on the
leader and the followers fall behind; read from a follower that hasn't caught up
and you've broken **read-your-writes**. Sync the follower and the read goes fresh
— watch the lag open and close:

<Visualizer algo="replication" />

## ACID vs BASE

- **ACID** (classic RDBMS): Atomicity, Consistency, Isolation, Durability —
  transactions are all-or-nothing and isolated. Great for money, orders,
  anything with invariants. StockStump's balance decrement needs this.
- **BASE** (many NoSQL): Basically Available, Soft state, Eventually consistent —
  trades strict guarantees for availability and scale.

The decision isn't ideological — it's per-workload. A trading balance is ACID; a
"who viewed your profile" counter is happily BASE.

<QA q="State the CAP theorem and what it means for a design decision.">

During a network partition you can't have both consistency and availability, so
you pick: CP (reject requests to avoid serving stale/divergent data) or AP (keep
serving and reconcile later). Since partitions are unavoidable, every
distributed datastore is effectively choosing CP or AP for its failure behavior.
In an interview I'd tie it to the data: money/inventory → CP; feeds/counters →
AP.

</QA>

<QA q="Give an example where eventual consistency is fine and one where it isn't.">

Fine: a tweet's like-count or "X people are viewing this" — if it's stale for a
few seconds nobody is harmed, and AP availability is more valuable. Not fine: a
bank balance or seat/inventory reservation — a stale read lets two users spend
or book the same thing, which is a correctness bug. Those need strong
consistency (a transaction or an atomic conditional update).

</QA>

<QA q="Why not make everything strongly consistent to be safe?">

Strong consistency costs latency and availability: it needs coordination
(consensus/round trips) and must reject writes during partitions. Forcing it
everywhere makes the whole system slower and less available than its weakest
guarantee needs to be. You scope strong consistency to the operations with hard
invariants and let the rest be eventually consistent.

</QA>

---

## Quorums & Split-Brain Mitigation

In a distributed cluster, how do we guarantee that two nodes don't both declare themselves the leader during a network partition? (This is the **split-brain problem**). We use **Quorums**.

### The Quorum Math

To read or write safely, a cluster of **N** nodes requires a majority agreement:

```text
Quorum Size = floor(N / 2) + 1
```

*   **For N = 3**: Quorum is 2. The cluster can tolerate 1 node failure.
*   **For N = 5**: Quorum is 3. The cluster can tolerate 2 node failures.
*   **Why odd numbers of nodes?** An odd number (e.g., 5) has the same failure tolerance as the next even number (e.g., 6, which also requires a quorum of 4 and can only tolerate 2 failures) but requires one fewer node, saving hardware costs and reducing network synchronization overhead.

To ensure a read always sees the latest write, the write quorum (**W**) and read quorum (**R**) must overlap:

```text
R + W > N
```

If this condition is met, any read quorum is guaranteed to contain at least one node that participated in the latest write quorum, ensuring strong consistency.

---

## Distributed Consensus: Paxos vs. Raft

To maintain a consistent state (like a distributed key-value store or transaction ledger) across multiple machines, we need a **Consensus Algorithm**.

### 1. Paxos (The Academic Classic)
Paxos operates in a leaderless or multi-proposer state, making it highly resilient but notoriously difficult to implement.
*   **Roles**: Proposer, Acceptor, Learner.
*   **Phase 1 (Prepare)**: A proposer sends a proposal number $N$ to a majority of acceptors. If acceptors agree, they promise not to accept any proposals numbered less than $N$.
*   **Phase 2 (Accept)**: Once the proposer receives promises from a majority, it sends the proposal value to those acceptors. If a majority accepts, the value is committed and sent to learners.

### 2. Raft (The Modern Standard)
Designed to be easier to understand than Paxos, Raft decomposes consensus into explicit sub-problems:
*   **Roles**: Leader, Follower, Candidate.
*   **Leader Election**: If a follower stops hearing from the leader (heartbeat timeout), it transitions to a candidate, increments the election term, and requests votes. A candidate needs a majority of votes to become the new leader.
*   **Log Replication**: All writes go through the leader. The leader writes the command to its log and sends it to all followers (AppendEntries). Once a majority of followers acknowledge, the leader commits the entry and responds to the client.

---

## Distributed Transactions: Two-Phase Commit (2PC) vs. Sagas

How do you update multiple microservices in a single transaction (e.g., debiting a bank account *and* booking a flight)?

### 1. Two-Phase Commit (2PC) — Strong Consistency
2PC relies on a central coordinator to force block updates across all participant databases.

```text
  Coordinator                  Service A (Database)         Service B (Database)
       │                                 │                            │
       ├──── Phase 1: Prepare? ──────────┼───────────────────────────>│
       │<─── Yes (Lock rows) ────────────┼────────────────────────────┤
       │                                 │                            │
       ├──── Phase 2: Commit! ───────────┼───────────────────────────>│
       │<─── Acknowledged (Unlock) ──────┼────────────────────────────┤
```

*   **Pros**: Guarantees ACID compliance across multiple databases.
*   **Cons**: **Blocking protocol**. If the coordinator dies midway, participants hold locks indefinitely, leading to resource starvation. Scales poorly due to network round-trips.

### 2. The Saga Pattern — Eventual Consistency
A Saga is a sequence of **local transactions**. Each service updates its own database and publishes an event. If a step fails, the saga executes **compensating transactions** (backward rollbacks) to undo preceding updates.

*   **Choreography**: Each service listens to events from other services and triggers its local action independently (decentralized).
*   **Orchestration**: A central orchestrator service tells each participant which local transaction to execute next, handling failures and triggering compensation steps directly (easier to debug).

```
         Saga Orchestrator
          /      |      \
     1.Debit  2.Book   [If 2 Fails]
     Account   Flight   3.Credit Account (Compensate)
```

*   **Pros**: Non-blocking, highly scalable, ideal for complex microservice architectures.
*   **Cons**: Lacks strict Isolation (intermediate states are visible to other transactions). Must be designed to handle out-of-order events.

---

## Design drills

CAP is a decision tool, not trivia. Practise *choosing* per workload.

<DesignDrills
  topic="CAP & consistency"
  drills={[
    {
      level: "Warm-up",
      prompt: "A network partition splits your replicas. For (a) a bank ledger and (b) a 'who's viewing this' counter, do you choose CP or AP — and what does the user experience?",
      covers: [
        "Ledger → CP: refuse the write/read rather than risk a double-spend (briefly unavailable).",
        "Counter → AP: keep serving, accept a stale/divergent number, reconcile later.",
        "Justify from the data's invariants, not a blanket rule.",
        "Name that when the network is healthy you get both — CAP only bites during a partition (PACELC for the else-case latency cost)."
      ]
    },
    {
      level: "Core",
      prompt: "A user updates their profile, then immediately reloads and sees the OLD value. Diagnose and fix without making the whole system strongly consistent.",
      covers: [
        "Replication lag → the reload hit a follower that hadn't applied the write (read-your-writes violation).",
        "Route that user's reads to the leader for a short window after their write …",
        "… or track the write position (LSN) and only read from a replica caught up to it.",
        "Scope the fix to that one read path; leave the rest eventually consistent."
      ]
    },
    {
      level: "Core",
      prompt: "Which of these need strong consistency and which are happily eventual: seat booking, like counts, account balance, a paginated feed?",
      covers: [
        "Strong: seat booking and account balance — invariants (no double-book / double-spend).",
        "Eventual: like counts (monotonic-ish, harmless if stale) and the feed (monotonic reads is enough).",
        "Map each to the spectrum row (strong / read-your-writes / monotonic / eventual).",
        "The point: consistency is chosen per-operation, not per-system."
      ]
    },
    {
      level: "Stretch",
      prompt: "Two users decrement the last unit of inventory at the same time on an AP store. What happens, and how do you make it safe?",
      covers: [
        "On AP both may read '1 left' and both succeed → oversell (the classic anomaly).",
        "Make the decrement an atomic conditional update (compare-and-set) or a transaction on a CP store for that operation.",
        "Or use a reservation + confirm with a TTL (the seat-lock pattern).",
        "Carve the one strongly-consistent operation out of an otherwise-AP system."
      ]
    }
  ]}
/>
