---
title: "Rate Limiting"
summary: "The five algorithms (fixed window, sliding log, sliding counter, token bucket, leaky bucket), how to make them work across many servers with Redis, and the trade-offs."
order: 7
tags: ["rate-limiting", "redis", "api-gateway"]
---

## Why & where

Rate limiting protects you from abuse, accidental floods, and cost blowups, and
enforces fair use / billing tiers. Put it at the **edge / API gateway** so bad
traffic dies before it touches your services. StockVision uses `slowapi` for
this and meters its B2B API keys; StockStump's per-player trade lock is a
specialized one-token limiter.

## The algorithms

| Algorithm | Idea | Trade-off |
|---|---|---|
| Fixed window | count per clock window (per minute) | cheap; **burst at the boundary** (2× at the edges) |
| Sliding log | store every request timestamp | exact; memory-heavy |
| Sliding window counter | weighted blend of current + previous window | great accuracy/cost balance |
| **Token bucket** | tokens refill at rate R, each request takes one | **allows controlled bursts**; the usual default |
| Leaky bucket | queue drains at fixed rate | smooths output to a constant rate |

**Token bucket** is the most common answer: it permits short bursts (up to the
bucket size) while bounding the long-run rate.

<Mermaid chart={`flowchart TB
  REFILL["Refill: R tokens/sec<br/>(steady drip)"] --> BUCKET[("Bucket<br/>capacity = B tokens<br/>(burst allowance)")]
  REQ[Request arrives] --> CHECK{token<br/>available?}
  BUCKET --> CHECK
  CHECK -->|"yes — take one"| PASS[✓ forward to service]
  CHECK -->|"no — bucket empty"| REJECT["✗ 429 + Retry-After"]
  style BUCKET fill:#E0E7FF,stroke:#4F46E5
  style REJECT fill:#FFE4E6,stroke:#E11D48
  style PASS fill:#DCFCE7,stroke:#16A34A`} />

The two knobs map directly to behavior: **R bounds the long-run rate**,
**B bounds the burst**. A full bucket after a quiet period is the "controlled
burst" — that's a feature, not a bug.

```python
import time

class TokenBucket:
    def __init__(self, rate, capacity):
        self.rate, self.capacity = rate, capacity
        self.tokens = capacity
        self.ts = time.monotonic()

    def allow(self, cost=1):
        now = time.monotonic()
        self.tokens = min(self.capacity, self.tokens + (now - self.ts) * self.rate)
        self.ts = now
        if self.tokens >= cost:
            self.tokens -= cost
            return True
        return False
```

Here it is running. The bucket starts full, so an opening burst of 5 passes
straight through; the 6th request in that burst is refused with a **429**, and
then the steady drip lets traffic through again. Change R, the capacity B, or the
arrival times and watch the burst-versus-throttle trade-off play out:

<Visualizer algo="token-bucket" />

## Distributed rate limiting

With many app servers, an in-memory counter per server multiplies the real
limit by the server count. So keep the counter in a **shared store (Redis)** and
mutate it atomically — a Lua script or `INCR` + `EXPIRE` makes the
read-modify-write a single atomic op, avoiding the same race that bites naive
balance checks.

<Mermaid chart={`flowchart LR
  C[Clients] --> GW[API gateway]
  GW --> S1[App server 1]
  GW --> S2[App server 2]
  GW --> S3[App server 3]
  S1 & S2 & S3 --> R[("Redis<br/>one shared counter per key<br/>atomic Lua / INCR+EXPIRE")]
  R -.-> NOTE["❌ without this: each server counts alone<br/>→ real limit = limit × N servers"]
  style R fill:#E0E7FF,stroke:#4F46E5
  style NOTE fill:#FFE4E6,stroke:#E11D48`} />



<Callout type="tip" title="Always return the right signal">
On limit, respond **429 Too Many Requests** with a `Retry-After` header so good
clients back off correctly. (LandAI's ingestion respects upstream `Retry-After`
for the same reason — be a polite client and expect politeness back.)
</Callout>

<QA q="Which rate-limiting algorithm would you pick and why?">

Token bucket as the default: it bounds the average rate while allowing short,
controlled bursts, which matches real traffic better than a hard cap. If I need
a perfectly smooth output rate (e.g. protecting a fragile downstream), leaky
bucket. Fixed-window is the simplest but suffers boundary bursts (a client can
send 2× the limit across the window edge); sliding-window counter fixes that
cheaply.

</QA>

<QA q="How do you rate-limit across 10 app servers without each enforcing its own count?">

Move the counter to a shared store like Redis and update it atomically — `INCR`
with an `EXPIRE`, or a Lua script for token-bucket math — so all servers see one
global count. Doing the increment atomically avoids the read-modify-write race
where concurrent requests both read an under-limit value and both pass. The
gateway then enforces one true limit regardless of how many app nodes there are.

</QA>

## Design drills

You know the five algorithms — now rehearse the *system* decisions an interviewer
actually probes.

<DesignDrills
  topic="Rate limiting"
  drills={[
    {
      level: "Warm-up",
      prompt: "A public API sells a 1,000 requests/minute tier that must tolerate short bursts. Pick an algorithm and set its knobs — what are R and B, and what do you return on reject?",
      covers: [
        "Token bucket: R ≈ 1000/60 ≈ 16.7 tokens/sec, with B sized to the burst you'll tolerate (say 100).",
        "R bounds the long-run rate; B bounds the burst — name both knobs explicitly.",
        "On reject: 429 Too Many Requests + a Retry-After header so well-behaved clients back off.",
        "Key the limiter per API key / per client, never as one global counter."
      ]
    },
    {
      level: "Core",
      prompt: "Now run it behind 10 gateway nodes. Where does the counter live, and how do you avoid the 'real limit = limit × 10' bug?",
      covers: [
        "Move the counter to a shared store (Redis); per-node in-memory counters multiply the limit by N.",
        "Make the read-modify-write atomic — INCR + EXPIRE, or a Lua script for token-bucket math — the same race as a balance double-spend.",
        "One key per client+window; let the key's TTL expire the window for you.",
        "Cost: a network hop per request — mitigate with a small per-node allowance synced asynchronously."
      ]
    },
    {
      level: "Core",
      prompt: "A customer complains they get throttled in bursts right at each minute boundary. Diagnose it and fix it without storing every timestamp.",
      covers: [
        "Fixed-window boundary burst: up to 2× the limit across the edge of two adjacent windows.",
        "Fix it cheaply with a sliding-window counter — weight the previous window's count by how much of it still overlaps now.",
        "Sliding log is exact but stores every timestamp — too memory-heavy at scale; name the trade-off."
      ]
    },
    {
      level: "Stretch",
      prompt: "Redis — your shared counter — goes down. What happens to request handling, and what should happen?",
      covers: [
        "Decide fail-open vs fail-closed deliberately: dropping all traffic to enforce a limit is usually worse than briefly over-allowing.",
        "Degrade to a local per-node limit so you still bound the blast radius while the store is gone.",
        "Wrap the store call in a timeout + circuit breaker; never let limiter latency become request latency.",
        "Emit metrics/alerts so the degradation is visible, not silent."
      ]
    },
    {
      level: "Stretch",
      prompt: "A multi-tenant SaaS needs per-user, per-organization, and global caps enforced at once. How do you layer them?",
      covers: [
        "Independent limiters with their own keys (user:123, org:42, global) — a request must pass every one that applies.",
        "Check the cheapest / most-specific first and short-circuit on the first reject to save round-trips.",
        "Consider priority lanes so one noisy tenant can't starve others under the global cap.",
        "Call out the cost: N limiters = N atomic ops per request; fold them into one Lua script if it matters."
      ]
    }
  ]}
/>
