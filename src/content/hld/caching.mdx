---
title: "Caching & CDNs"
summary: "Where to cache, what to cache, how to invalidate, and the failure modes (stampede, penetration, avalanche) interviewers love to probe."
order: 2
tags: ["performance", "redis", "cdn", "scalability"]
---

## The layers

A request can be served from several caches before it ever touches your
database. Push the cache as close to the user as you can afford to.

<Mermaid chart={`flowchart LR
  U[Client] --> B[Browser cache]
  B --> E[CDN / edge]
  E --> G[API gateway]
  G --> A[App server]
  A --> R[(Redis)]
  A --> D[(Database)]
  R -. miss .-> D`} />

| Layer | Caches | TTL feel | Invalidation |
|---|---|---|---|
| Browser | static assets, GET responses | minutes–days | `Cache-Control`, content-hash filenames |
| CDN / edge | images, JS/CSS, cacheable API GETs | minutes–days | purge API, versioned URLs |
| Application (Redis) | hot rows, computed views, sessions | seconds–hours | TTL + explicit delete on write |
| Database | buffer pool, query cache | — | engine-managed |

## Read strategies

- **Cache-aside (lazy):** app checks cache → on miss, reads DB and populates.
  Simple, resilient, the default. Risk: first request after expiry is slow.
- **Read-through:** the cache library loads from DB on miss. Cleaner app code,
  couples you to the cache layer.
- **Write-through / write-behind:** write to cache and DB together (through) or
  flush async (behind). Behind is fast but can lose writes on crash.

Watch the hit/miss mechanic play out. The cache is cold at first — every request
is a miss and a slow trip to the store — then it warms up: repeated keys are
served from memory, and once it's full the least-recently-used key is evicted to
make room. That climbing hit-rate is what every caching decision is really after:

<Visualizer algo="cache" />

<Callout type="warn" title="The three failure modes to name">
**Stampede / dogpile** — a hot key expires and 10k requests hit the DB at once.
Fix: per-key lock or "early recompute" with a probabilistic TTL.
**Penetration** — queries for keys that don't exist bypass the cache every time.
Fix: cache the negative result (short TTL) or a Bloom filter.
**Avalanche** — many keys share an expiry instant. Fix: jitter the TTLs.
</Callout>

## Invalidation

> "There are only two hard things in Computer Science: cache invalidation and
> naming things." — Phil Karlton

Practical rules: prefer **TTL + explicit delete on write** over trying to keep
caches perfectly coherent. Use **content-hashed URLs** for static assets so you
never invalidate — you just stop referencing the old file. For derived data,
store a **version stamp** and bump it on write.

<QA q="A product page is read 100k times/min and updated a few times a day. How do you cache it?">

Cache-aside in Redis with a generous TTL (say 5–10 min) plus **explicit
invalidation on the rare write** (delete or overwrite the key in the update
path). Put the static assets behind a CDN with content-hashed filenames.
Because writes are rare, coherence is cheap; because reads are huge, the hit
rate is ~100%. Add TTL jitter to avoid avalanche and a per-key lock to avoid a
stampede when the key does expire.

</QA>

<QA q="Why not just cache everything forever?">

Memory is finite and stale data is a correctness bug. You need an eviction
policy (usually **LRU/LFU**) so the cache holds the *working set*, and a TTL so
data that changes out-of-band doesn't live forever. "Cache everything forever"
also makes invalidation the only lever you have — and invalidation is the hard
part.

</QA>

## Design drills

You can recite cache-aside — now defend the decisions under pressure.

<DesignDrills
  topic="Caching"
  drills={[
    {
      level: "Warm-up",
      prompt: "A product page is read 100k times/min and edited a few times a day. Design its caching — layers, TTL, and invalidation.",
      covers: [
        "Cache-aside in Redis with a generous TTL (5–10 min): reads dwarf writes, so coherence is cheap and the hit rate is ~100%.",
        "Invalidate explicitly on the rare write (delete/overwrite the key in the update path).",
        "Static assets behind a CDN with content-hashed filenames — no invalidation needed, just stop referencing the old file.",
        "Add TTL jitter (avoid avalanche) + a per-key lock or early recompute (avoid stampede)."
      ]
    },
    {
      level: "Core",
      prompt: "A hot key expires and 10,000 requests hit the DB in the same second. Name the failure and stop it.",
      covers: [
        "This is a cache stampede / dogpile.",
        "Per-key mutex: only one request recomputes the value; the rest wait or serve stale.",
        "Probabilistic early recompute — refresh just before expiry so the key rarely fully expires.",
        "Serve-stale-while-revalidate as a backstop."
      ]
    },
    {
      level: "Core",
      prompt: "Attackers query millions of IDs that don't exist; each one sails past the cache to the DB. Diagnose and fix.",
      covers: [
        "Cache penetration — misses for non-existent keys are never cached, so every one hits the DB.",
        "Cache the negative result with a short TTL.",
        "Front the lookup with a Bloom filter to reject definitely-absent keys cheaply.",
        "Validate/normalize inputs so absurd keys die at the edge."
      ]
    },
    {
      level: "Stretch",
      prompt: "Choose write-through vs write-back vs cache-aside for (a) a user's session and (b) a financial ledger. Justify each.",
      covers: [
        "Cache-aside: the resilient default — the cache can fail without losing data.",
        "Write-through: cache + DB written together; strong consistency, slower writes — fine for a session you must not drop.",
        "Write-back: fast, batches to the DB, but loses writes on crash — never for a ledger.",
        "Ledger → durable store of record first; cache only derived reads."
      ]
    },
    {
      level: "Stretch",
      prompt: "Reads are global but your cache lives in one region. Serve a worldwide audience without stale chaos.",
      covers: [
        "Edge/CDN caching for cacheable GETs; regional Redis for application cache.",
        "Decide per data type: static → long TTL at the edge; user-specific → short TTL or no cache.",
        "Cross-region invalidation via a purge API, versioned URLs, or pub-sub eviction events.",
        "Accept bounded staleness explicitly — the TTL is your consistency knob."
      ]
    }
  ]}
/>
