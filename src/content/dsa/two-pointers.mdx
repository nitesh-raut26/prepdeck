---
title: "Two Pointers"
summary: "Opposite-end, fast/slow, and partition variants — replace nested loops on sorted/linked data with a single O(n) pass."
order: 14
difficulty: "Core pattern"
tags: ["arrays", "linked-list", "two-pointers"]
---

## Why We Need Two Pointers & How to Think About It
In many array and string problems, a naive search requires checking all pairs of elements. This results in nested loops and an $O(n^2)$ time complexity. 
The **Two Pointers** pattern optimizes this to $O(n)$ by maintaining two independent indices (pointers) that traverse the data structure. Because we can prove that moving a pointer *discards* a set of invalid possibilities without checking them, we never need to re-scan.

---

## Three Core Variants

### 1. Opposite Ends (Converging Pointers)
* **How it works**: Start one pointer at index `0` and another at `n - 1`. Move them toward each other based on a condition until they meet.
* **Why it works**: If the array is **sorted**, a comparison of the values at the two pointers tells us if we need to make our sum larger (move left pointer up) or smaller (move right pointer down). We safely discard the other options.
* **When to use**: Pair sum in sorted array, palindrome verification, container with most water.
  - *LeetCode Link*: [LeetCode 167: Two Sum II - Input Array Is Sorted](https://leetcode.com/problems/two-sum-ii-input-array-is-sorted/)
  - *LeetCode Link*: [LeetCode 11: Container With Most Water](https://leetcode.com/problems/container-with-most-water/)

### 2. Fast & Slow (Runner Technique)
* **How it works**: Two pointers start at the same position but move at different speeds (usually `slow` moves 1 step, `fast` moves 2 steps).
* **Why it works**: If there is a cycle (e.g. in a linked list), the fast pointer will eventually lap the slow pointer and they will meet. If there is no cycle, the fast pointer will hit the end of the list.
* **When to use**: Cycle detection, finding the middle node of a list, finding the k-th node from the end.
  - *LeetCode Link*: [LeetCode 141: Linked List Cycle](https://leetcode.com/problems/linked-list-cycle/)
  - *LeetCode Link*: [LeetCode 876: Middle of the Linked List](https://leetcode.com/problems/middle-of-the-linked-list/)

### 3. Partition / Same-Direction (Reader/Writer)
* **How it works**: Both pointers start at the beginning and move in the same direction. A `read` pointer scans elements, and a `write` pointer tracks where the next valid element should go.
* **When to use**: In-place filtering, removing duplicates, partitioning arrays (like Dutch National Flag or Quicksort partition).
  - *LeetCode Link*: [LeetCode 27: Remove Element](https://leetcode.com/problems/remove-element/)
  - *LeetCode Link*: [LeetCode 283: Move Zeroes](https://leetcode.com/problems/move-zeroes/)

```python
# Opposite ends: does a SORTED array contain two numbers summing to target?
def two_sum_sorted(a, target):
    lo, hi = 0, len(a) - 1
    while lo < hi:
        s = a[lo] + a[hi]
        if s == target: return (lo, hi)
        if s < target:  lo += 1     # need bigger → move left pointer up
        else:           hi -= 1     # need smaller → move right pointer down
    return None
```

Why it's O(n): each pointer only moves one direction, so together they take ≤ n
steps. The "sorted" precondition is what lets a single comparison decide which
pointer to advance.

## Watch it run

Converging pointers on a sorted array: every comparison moves exactly one
pointer inward, so the whole scan is a single pass. Try a target with no
valid pair and watch the pointers meet empty-handed.

<Visualizer algo="two-pointers" />

## Fast & slow — cycle detection (Floyd's)

```python
def has_cycle(head):
    slow = fast = head
    while fast and fast.next:
        slow = slow.next          # +1
        fast = fast.next.next     # +2
        if slow is fast: return True   # they meet ⇒ cycle
    return False
```

<Callout type="tip" title="Two pointers vs hashing">
A hash set also finds a pair-sum in O(n) time but O(n) space. Two pointers is
O(1) space *if the array is already sorted*. If it isn't, sorting first is O(n
log n) — then a hash-set pass at O(n) may win. State the trade-off out loud.
</Callout>

<QA q="Two pointers vs a hash set for the two-sum problem — which and when?">

If the array is already sorted, two pointers solves it in O(n) time and O(1)
space — strictly better on memory. If it's unsorted, a hash set finds the
complement in O(n) time but O(n) space without sorting, whereas two pointers
needs an O(n log n) sort first. So: sorted (or sorting is free/needed anyway) →
two pointers; unsorted and you want to avoid the sort → hash set.

</QA>

<QA q="How does Floyd's cycle detection work and why is it O(1) space?">

A slow pointer moves one step, a fast pointer two. If there's a cycle the fast
pointer laps the slow one and they meet inside the loop; if `fast` reaches null
there's no cycle. It uses two pointers and no extra structure, so it's O(1)
space versus O(n) for a visited-set approach. To find the cycle's start, reset
one pointer to the head and advance both at the same speed until they meet again.

</QA>

## Think it through

The whole pattern lives or dies on one question: *why is it safe to throw
away a pointer's current position?* Reason through the cleanest example of
that argument before revealing anything.

<ThinkThrough
  title="Container With Most Water"
  difficulty="Medium — LeetCode 11"
  problem="Heights = [1,8,6,2,5,4,8,3,7]. Each value is a vertical wall. Pick two walls; the water they hold is min(left, right) × distance between them. Return the maximum. (Answer: 49, between the 8 at index 1 and the 7 at index 8.)"
  stages={[
    {
      label: "Restate & edges",
      prompt: "What exactly am I maximizing, and what bounds the area of any pair?",
      reveal: "Area = min(h[i], h[j]) × (j − i). Two knobs fight each other: width (j − i) and the SHORTER of the two walls (water spills over the lower one). Edges: fewer than two walls → 0; equal heights are allowed; flat input like [3,3,3]."
    },
    {
      label: "Brute force first",
      prompt: "Dumbest correct answer and its cost?",
      reveal: "Try all pairs: two nested loops, O(n²) time, O(1) space. Fine for small n, too slow for n = 10⁵. The repeated work: I keep re-measuring pairs that can't beat what I've seen. Can I start from the pair with the most width and shed possibilities provably?"
    },
    {
      label: "The key move",
      prompt: "Start at both ends (widest base). The shorter wall limits the area — so which pointer is safe to move inward?",
      reveal: "Move the pointer at the SHORTER wall. Here's the proof that makes this a pattern, not a guess: the area is capped by the shorter wall. If I move the TALLER wall inward, width shrinks AND the cap (the shorter wall) can't rise — so no pair I skip could have been better. Only moving the shorter wall has any chance of lifting the cap. Each step retires one wall safely → O(n)."
    },
    {
      label: "Code the template",
      prompt: "Converge from both ends; advance the shorter side. What about a tie?",
      reveal: "On a tie, move either side — neither choice can skip a strictly better pair (both walls are the limiter, so shrinking width on either side loses width without a chance to raise the cap).",
      code: "def max_area(h):\n    lo, hi = 0, len(h) - 1\n    best = 0\n    while lo < hi:\n        area = min(h[lo], h[hi]) * (hi - lo)\n        best = max(best, area)\n        if h[lo] < h[hi]:    # shorter wall on the left\n            lo += 1          # only moving it can raise the cap\n        else:\n            hi -= 1\n    return best"
    },
    {
      label: "Cost & edge check",
      prompt: "Cost, and what makes the discard argument airtight?",
      reveal: "O(n) time, O(1) space — each pointer moves inward at most n times. The airtight part: every pointer move discards exactly the pairs that provably can't beat the current best, because the shorter wall caps them. Memorize that sentence — 'I can discard this side because it can never improve the answer' is the two-pointer pattern in every problem it appears in."
    }
  ]}
/>

## Check yourself

<Quiz questions={[
  {
    q: "Container With Most Water: the two walls are equal height. Which pointer should you move?",
    options: [
      "Either — when they're equal, neither move can skip a strictly better pair",
      "Always the left pointer",
      "Always the right pointer",
      "Stop — equal walls mean you've found the maximum"
    ],
    answer: 0,
    explain: "Both walls are the limiter, so shrinking the width on either side loses width with no chance to raise the cap. Picking a side by convention is fine; correctness doesn't depend on which."
  },
  {
    q: "Why is the opposite-ends two-pointer scan O(n) rather than O(n²)?",
    options: [
      "Each pointer only moves toward the other, so together they take at most n steps",
      "Because the input is sorted, which makes every step O(1)",
      "Because it allocates no extra memory",
      "Because min() and max() are constant time"
    ],
    answer: 0,
    explain: "lo only increases and hi only decreases; they meet after at most n moves total. Linear work, no nesting — that's the whole speedup over checking all pairs."
  },
  {
    q: "Two-sum on an UNSORTED array, and you want O(1) extra space. Can opposite-ends two pointers do it directly?",
    options: [
      "No — opposite-ends needs sorted order; you'd pay O(n log n) to sort first",
      "Yes, it works on any array as-is",
      "Yes, as long as you move both pointers each step",
      "No, two pointers can never solve two-sum"
    ],
    answer: 0,
    explain: "The 'move the smaller sum's pointer up' decision is only valid when the array is sorted. Unsorted, either sort first (O(n log n)) or use a hash set (O(n) time, O(n) space). Naming that trade-off out loud is the senior signal."
  },
  {
    q: "Floyd's fast/slow pointers return True on a linked list. What does that mean, and what did it cost?",
    options: [
      "There's a cycle; O(1) extra space — no visited set needed",
      "There's a cycle; O(n) space for the visited set",
      "The list is sorted; O(1) space",
      "The list is a palindrome; O(n) space"
    ],
    answer: 0,
    explain: "A faster pointer must eventually lap a slower one inside a loop, so meeting ⇒ cycle. The win over a hash set of visited nodes is memory: O(1) instead of O(n)."
  }
]} />

## Practice — climb the ladder

Two pointers earns its keep on SORTED data or paired ends: each step
discards possibilities provably, which is where the O(n) comes from.

<PracticeLadder
  topic="Two Pointers"
  tiers={[
    {
      label: "Warm-up",
      note: "ends moving inward, slow/fast moving forward",
      problems: [
        { name: "Valid Palindrome", lc: "valid-palindrome", difficulty: "Easy", trains: "Ends-inward with skip rules — the cleanest convergent-pointer drill." },
        { name: "Reverse String", lc: "reverse-string", difficulty: "Easy", trains: "Swap-and-converge — in-place mutation with two indices." },
        { name: "Move Zeroes", lc: "move-zeroes", difficulty: "Easy", trains: "Reader/writer pointers — stable partition in one pass." }
      ]
    },
    {
      label: "Core",
      note: "the discard argument, made explicit",
      problems: [
        { name: "Container With Most Water", lc: "container-with-most-water", difficulty: "Medium", trains: "Move the SHORTER wall — articulate why that discard is safe; that argument IS the pattern." },
        { name: "3Sum", lc: "3sum", difficulty: "Medium", trains: "Sort + fix one + two-pointer the rest, with dedup discipline — the interview staple." },
        { name: "Remove Duplicates from Sorted Array II", lc: "remove-duplicates-from-sorted-array-ii", difficulty: "Medium", trains: "Writer pointer with a look-back condition — in-place with a twist." }
      ]
    },
    {
      label: "Stretch",
      note: "maximum discard sophistication",
      problems: [
        { name: "4Sum", lc: "4sum", difficulty: "Medium", trains: "The 3Sum skeleton generalized — nested fixing without drowning in dedup." },
        { name: "Trapping Rain Water", lc: "trapping-rain-water", difficulty: "Hard", trains: "Two pointers with running maxes from both ends — the famous finale." }
      ]
    }
  ]}
/>
