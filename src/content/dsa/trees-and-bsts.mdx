---
title: "Trees & BSTs"
summary: "Hierarchy as a data structure — tree vocabulary, the four traversals, binary search trees, and why balance is everything."
order: 7
difficulty: "Foundation"
tags: ["trees", "bst", "recursion", "fundamentals"]
---

## When data has hierarchy

Arrays and lists are flat. But file systems, org charts, HTML pages
([the DOM](/basics/websites-and-browsers)), comment threads, JSON — real
data nests. A **tree** is the structure for hierarchy: nodes connected
parent-to-child, no cycles, one **root** at the top.

```
            (root) Documents
           /        |        \
       Photos     Work      Music
       /    \       |
   2025    2026   resume.pdf     ← leaves: nodes with no children
```

Vocabulary you'll use forever: **root** (top), **parent/child** (direct
links), **leaf** (no children), **subtree** (any node + its descendants —
itself a tree, which is *why recursion fits*), **depth** (distance from
root), **height** (longest root-to-leaf path).

A **binary tree** allows at most two children per node — `left` and
`right` — and is the form 90% of interview tree questions take:

```python
class TreeNode:
    def __init__(self, val):
        self.val = val
        self.left = None      # both are TreeNodes or None —
        self.right = None     # a node's children are themselves trees
```

That self-similarity is the key mental shift of this page: **every tree
algorithm is "do something with this node; recurse on the subtrees."** The
[call stack](/programming/functions) does the bookkeeping.

```python
def height(node):
    if node is None:                 # base case: empty tree has height -1
        return -1
    return 1 + max(height(node.left), height(node.right))

def count(node):
    if node is None:
        return 0
    return 1 + count(node.left) + count(node.right)
```

Two lines of pattern — null check, combine recursive answers — and you can
already compute size, height, sums, mirror a tree, compare two trees…

## The four traversals

Same tree, four different visit orders — the only question is *when each
node gets to speak*. Flip between them and watch the output row; notice
in-order reading the BST in sorted order, and level-order being plain BFS:

<Visualizer algo="tree-traversal" />

"Visit every node" has four standard orders, and naming them is interview
table stakes:

| Traversal | Order | Use it for |
| --- | --- | --- |
| **Pre-order** | node, left, right | copy/serialize a tree |
| **In-order** | left, node, right | **sorted order in a BST** ← the famous one |
| **Post-order** | left, right, node | delete/free, compute from children up |
| **Level-order** | top to bottom, row by row | shortest paths, "by level" output |

```python
def inorder(node, out):
    if node is None:
        return
    inorder(node.left, out)
    out.append(node.val)
    inorder(node.right, out)
```

The first three are **DFS** (depth-first — dive, then backtrack: a
[stack discipline](/dsa/stacks-and-queues), via recursion). Level-order is
**BFS** (breadth-first — a queue holding the current frontier):

```python
from collections import deque

def level_order(root):
    if not root: return []
    out, q = [], deque([root])
    while q:
        node = q.popleft()
        out.append(node.val)
        if node.left:  q.append(node.left)
        if node.right: q.append(node.right)
    return out
```

These are the same DFS/BFS you'll meet on [graphs](/dsa/graphs-bfs-dfs) —
a tree is just a graph with no cycles, so trees are where you learn them
gently.

## Binary Search Trees: sorted order as a shape

A **BST** adds one rule to a binary tree: at every node,
**left subtree < node < right subtree.** That single invariant turns the
tree into a decision diagram for search:

```
            8
          /   \
         3     10        find 6: 6<8 go left, 6>3 go right,
        / \      \                6=6 found — 3 comparisons,
       1   6     14               not a scan
```

Each comparison discards an entire subtree — the same halving as
[binary search](/dsa/binary-search), but in a structure that also supports
**insertion and deletion** without shifting anything
(unlike a sorted [array](/dsa/arrays-and-strings)):

| Operation | Sorted array | Hash table | **Balanced BST** |
| --- | --- | --- | --- |
| Search | O(log n) | O(1) | O(log n) |
| Insert/delete | O(n) — shift | O(1) | **O(log n)** |
| Min / max | O(1) | O(n) | O(log n) |
| **Sorted iteration / range query** | O(n) ✓ | ✗ no order | **O(n) ✓ in-order** |
| "All keys between a and b" | O(log n + k) | ✗ | **O(log n + k)** |

Read that table's last two rows — they're the BST's reason to exist:
**hash tables answer exact lookups; BSTs answer *order* questions** (min,
max, range, nearest, rank) while staying fast to modify.

```python
def search(node, target):
    if node is None or node.val == target:
        return node
    if target < node.val:
        return search(node.left, target)
    return search(node.right, target)
```

## Watch it run

Build a BST by inserting values one at a time, then search — every comparison
discards a whole subtree. Now change the insert order to `1, 2, 3, 4, 5, 6, 7`
and watch the next section's warning come true.

<Visualizer algo="bst" />

## The catch: balance

Insert 1, 2, 3, 4, 5 in order and the "tree" becomes a chain hanging right —
height n, every operation O(n). A [linked list](/dsa/linked-lists) with
extra steps. All the O(log n) promises hold only if height stays ~log n:
a **balanced** tree.

**Self-balancing BSTs** — AVL trees, Red-Black trees — restructure
themselves with O(1)-ish *rotations* on every insert/delete to guarantee
O(log n) height. You will almost never implement one (say that confidently
in interviews — knowing *that* is the senior answer), but you use them
daily: Java's `TreeMap`, C++'s `std::map`/`std::set` are Red-Black trees.
And databases use the same idea, generalized: the **B-tree** — a BST
flattened into fat nodes matching disk pages — is *the* database index
([indexing, Level 9](/fundamentals/databases); it's why `ORDER BY` and
range scans are fast).

## Production perspective

- Every `std::map`, `TreeMap`, database index, and filesystem directory
  structure is this page. The DOM your browser renders, the JSON your API
  returns, the org chart in HR software — trees.
- **Heaps** ([next-but-one page](/dsa/heaps-and-intervals)) are binary trees
  with a *different* invariant (parent beats children) stored in an array —
  same shape, different promise.
- Compilers parse your code into a tree (the AST) and every analysis is a
  traversal — when Level 8 tooling (linters, bundlers) feels magical, it's
  post-order walks.

## Common mistakes

- **Forgetting the base case** — `if node is None: return ...` is line one
  of every tree function; omit it and you recurse into a crash.
- **Checking only children, not subtrees, for BST validity** — `5 → right:
  8 → left: 3` passes the child check but breaks the invariant (3 < 5 yet
  sits in 5's right subtree). Validate with *bounds* passed down
  (see QA below) — this is among the most-failed easy questions.
- **Confusing depth and height**, or height of empty tree (-1) vs leaf (0)
  — define it out loud before coding.
- **Assuming BST when given "binary tree"** — no order invariant means no
  binary-search shortcuts; read the problem.
- **Recursion depth** — a chain-shaped tree of 10⁵ nodes overflows Python's
  default recursion limit; the iterative-with-stack version exists for a
  reason.

## Think it through

The page's ladder flags one habit as the senior tree skill: a node *returns*
one value to its parent while you *track* a different answer on the side.
Reason through the cleanest example before revealing.

<ThinkThrough
  title="Diameter of a Binary Tree"
  difficulty="Easy-Medium — LeetCode 543"
  problem="The diameter is the number of EDGES on the longest path between any two nodes — and that path need NOT pass through the root. Return it."
  stages={[
    {
      label: "Restate & edges",
      prompt: "Where does the longest path 'bend', and what's the answer for a single node?",
      reveal: "Any path has a single highest point — the node where it bends from going up to going down. At that apex node, the path = (deepest reach on the left) + (deepest reach on the right). Edges: empty tree → 0; one node → 0 (no edges)."
    },
    {
      label: "Brute force first",
      prompt: "Obvious solution and why it's slow?",
      reveal: "For every node, compute height(left) + height(right) — the longest path bending there — and take the max over all nodes. Correct, but each height() call is its own O(n) walk, so computing it at all n nodes is O(n²). The repeated work: heights are recomputed from scratch at every node."
    },
    {
      label: "Find the pattern",
      prompt: "I'm already computing height in a post-order walk. What can I piggyback on it?",
      reveal: "Compute height in ONE post-order pass. At each node you already have lh and rh (its children's heights) — so update a running best diameter = lh + rh + 2 right there, then RETURN the node's own height to its parent. Return one thing (height), track another (best). That split is the whole technique."
    },
    {
      label: "Code the template",
      prompt: "Why return height but accumulate 'best' in an outer variable?",
      reveal: "The parent needs only the height to compute its own path; the diameter is a global maximum that can occur at any apex, so it lives outside the recursion (nonlocal) and is updated at every node.",
      code: "def diameter(root):\n    best = 0\n    def height(node):            # height in EDGES; also updates best\n        nonlocal best\n        if node is None:\n            return -1            # empty = -1, so a leaf reports height 0\n        lh = height(node.left)\n        rh = height(node.right)\n        best = max(best, lh + rh + 2)  # longest path bending at this node\n        return 1 + max(lh, rh)\n    height(root)\n    return best"
    },
    {
      label: "Cost & edge check",
      prompt: "Cost, and how does this idea generalize?",
      reveal: "O(n) time (each node visited once), O(h) recursion stack. The same return-vs-track shape solves Maximum Path Sum (return the best downward arm, track the best bend that may dip into negatives) and many others — once you can name 'what do I return to my parent vs what do I record globally', hard tree problems collapse."
    }
  ]}
/>

## Check yourself

<Quiz questions={[
  {
    q: "In-order traversal (left, node, right) of a BST produces the values in:",
    options: [
      "Sorted ascending order",
      "Insertion order",
      "Level-by-level order",
      "Reverse-sorted order"
    ],
    answer: 0,
    explain: "Visiting the smaller (left) subtree before the node before the larger (right) subtree yields keys in increasing order. This single fact is behind 'kth smallest in a BST' and BST validation by traversal."
  },
  {
    q: "To validate a BST, why is checking each node only against its immediate children wrong?",
    options: [
      "The invariant is about whole subtrees — a node deep in a right subtree can still violate an ancestor's bound; pass (low, high) bounds down instead",
      "Children might be null, which crashes the check",
      "It's correct but runs in O(n²)",
      "Nothing is wrong — child comparisons are sufficient"
    ],
    answer: 0,
    explain: "Example: 5 with right child 8 whose left child is 3. Child checks pass, but 3 sits in 5's right subtree and breaks 'everything right of 5 is > 5'. Bounds (or an in-order monotonicity check) catch it."
  },
  {
    q: "You insert 1, 2, 3, 4, 5 into a plain BST in that order. What's the height and why?",
    options: [
      "Height n−1 — it degenerates into a right-leaning chain, so operations become O(n)",
      "Height ≈ log n — BSTs rebalance themselves on insert",
      "Height 0 — everything lands on one level",
      "It raises a duplicate-key error"
    ],
    answer: 0,
    explain: "A plain BST does NOT self-balance; sorted input is its worst case, producing a linked list. Guaranteed O(log n) height is exactly what AVL and Red-Black trees buy with rotations."
  },
  {
    q: "Why do databases index with B-trees rather than hash tables?",
    options: [
      "B-trees keep keys ordered, so they serve range queries (BETWEEN, ORDER BY); hash tables destroy order",
      "B-trees use less memory than hash tables",
      "Hash tables can't store integer keys",
      "B-trees give O(1) point lookups"
    ],
    answer: 0,
    explain: "Order questions (ranges, sorting, nearest) are everywhere in SQL, and only an ordered structure answers them. B-trees also pack many keys per disk page, staying 3–4 levels deep for millions of rows."
  }
]} />

## Interview perspective

<QA q="Validate that a binary tree is a BST — and what's wrong with the obvious approach?">

The obvious approach — check each node against its immediate children —
fails because the invariant is about *entire subtrees*: every node in the
left subtree must be smaller, not just the left child. Correct: recurse with
a (low, high) window — root gets (−∞, +∞); going left tightens high to
node.val, going right tightens low. Each node checks `low < val < high`.
O(n) time, O(height) stack. Alternative: an in-order traversal of a valid
BST is strictly increasing — walk it and verify order. Both answers are
accepted; knowing *why* the naive one fails is the actual test.

</QA>

<QA q="Why do databases use B-trees rather than hash tables or plain BSTs for indexes?">

Indexes must support range queries (`WHERE age BETWEEN 25 AND 30`,
`ORDER BY`) — hash tables can't, they destroy order. A plain BST keeps order
but its nodes are scattered, and on disk every pointer-hop is a slow page
read ([the speed gap](/basics/how-computers-work)). B-trees fatten each node
to hold hundreds of keys sized to a disk page, making the tree only 3–4
levels deep for millions of rows — a handful of page reads per lookup, plus
ordered leaves for range scans. Hash indexes do exist for pure
exact-match workloads; the default is B-tree because order questions are
everywhere. (Full story in Level 9.)

</QA>

<QA q="Find the lowest common ancestor (LCA) of two nodes in a BST. Why is the BST version easier than the general one?">

Walk from the root: if both targets are smaller, go left; both larger, go
right; otherwise — they split here (or one equals the node) — this node is
the LCA. O(height), no extra space, because the BST invariant tells you
*which side* each node lives on. In a plain binary tree there's no compass:
you recurse both sides and combine ("if left and right each found one
target, I'm the ancestor") — O(n). One problem, two structures, two
difficulties — a perfect illustration of what invariants buy.

</QA>

## Practice

1. **Warm-up (recursion reps):** implement `height`, `count`, `sum`, and
   `mirror` (swap every left/right). Each is ≤5 lines; do them from memory.
2. **Traversals:** build the BST from the diagram above by inserting
   `8,3,10,1,6,14`; print all four traversals; confirm in-order comes out
   sorted.
3. **The canon:** validate-BST (bounds method), level-order traversal
   grouped by level (the output is `[[8],[3,10],[1,6,14]]` — where does the
   per-level boundary come from?), LCA both ways.
4. **Think in invariants:** you need a leaderboard supporting "insert
   score," "delete score," and "how many scores are higher than X?" —
   compare hash table, sorted array, and BST honestly, then pick.
   (The rank query is the tell.)

Next: [Tries](/dsa/tries) — trees whose *paths* are the data, powering
autocomplete everywhere.

## Practice — climb the ladder

Almost every tree problem is "pick a traversal + decide what each node
returns to its parent". Say those two choices out loud before coding.

<PracticeLadder
  topic="Trees & BSTs"
  tiers={[
    {
      label: "Warm-up",
      note: "recursion shape drills",
      problems: [
        { name: "Maximum Depth of Binary Tree", lc: "maximum-depth-of-binary-tree", difficulty: "Easy", trains: "The smallest possible tree recursion — base case + combine children." },
        { name: "Invert Binary Tree", lc: "invert-binary-tree", difficulty: "Easy", trains: "Mutate-and-recurse — the famous one; swap, then trust recursion." },
        { name: "Same Tree", lc: "same-tree", difficulty: "Easy", trains: "Recursing on TWO trees in lockstep — structural comparison." }
      ]
    },
    {
      label: "Core",
      note: "traversal choice + BST ordering",
      problems: [
        { name: "Binary Tree Level Order Traversal", lc: "binary-tree-level-order-traversal", difficulty: "Medium", trains: "BFS with a queue, level by level — the non-recursive traversal you must own." },
        { name: "Validate Binary Search Tree", lc: "validate-binary-search-tree", difficulty: "Medium", trains: "Passing (min, max) bounds down — the trap is checking only children, not ranges." },
        { name: "Kth Smallest Element in a BST", lc: "kth-smallest-element-in-a-bst", difficulty: "Medium", trains: "In-order traversal IS sorted order — the one fact that defines BSTs." },
        { name: "Lowest Common Ancestor of a BST", lc: "lowest-common-ancestor-of-a-binary-search-tree", difficulty: "Medium", trains: "Using ordering to walk down in O(h) — no parent pointers needed." },
        { name: "Diameter of Binary Tree", lc: "diameter-of-binary-tree", difficulty: "Easy", trains: "Return one thing (height), update another (best) — the return-vs-track split." }
      ]
    },
    {
      label: "Stretch",
      note: "the senior-loop pair",
      problems: [
        { name: "Binary Tree Maximum Path Sum", lc: "binary-tree-maximum-path-sum", difficulty: "Hard", trains: "Diameter's idea with negatives — what a node CONTRIBUTES vs what passes THROUGH it." },
        { name: "Serialize and Deserialize Binary Tree", lc: "serialize-and-deserialize-binary-tree", difficulty: "Hard", trains: "Preorder + null markers — turning structure into a stream and back." }
      ]
    }
  ]}
/>
