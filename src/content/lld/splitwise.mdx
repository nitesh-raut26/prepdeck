---
title: "Design Splitwise"
summary: "Expense modeling with Strategy for split types, the balance-sheet invariant, and the debt-simplification algorithm everyone gets asked."
order: 13
difficulty: "Classic"
tags: ["LLD", "OOD", "strategy-pattern", "graphs"]
---

## Scope it first

*"Users create group expenses — one payer, many participants, split
equally / by exact amounts / by percentages. Show who owes whom; simplify
debts. Single currency, ignore payments/settlement execution and auth —
OK?"* That scope hits everything graders look for: a Strategy seam, a
money-correctness invariant, and one genuine algorithm.

## Entities & relationships

<Mermaid chart={`classDiagram
  class ExpenseManager {
    +addExpense(paidBy, amount, participants, strategy)
    +balanceOf(user)
    +simplify(group) List~Transaction~
  }
  class Expense {
    +paidBy User
    +amount Money
    +splits List~Split~
  }
  class Split {
    +user User
    +share Money
  }
  class SplitStrategy {
    <<interface>>
    +computeSplits(amount, participants, args) List~Split~
  }
  class BalanceSheet {
    +net Map~User, Money~
    +apply(expense)
  }
  ExpenseManager --> Expense : records
  ExpenseManager --> BalanceSheet : updates
  Expense --> "*" Split
  SplitStrategy <|.. EqualSplit
  SplitStrategy <|.. ExactSplit
  SplitStrategy <|.. PercentSplit
  Expense --> SplitStrategy : built via`} />

- **`Expense`** is an immutable record: who paid, how much, and the
  computed `Split`s. Edits create a new version (audit trail — finance
  code never destroys history).
- **`SplitStrategy`** — equal / exact / percent are interchangeable
  algorithms producing `List<Split>`: the cleanest Strategy showcase in
  any LLD classic. New split type (by shares, by adjustment) = new class,
  zero edits (Open/Closed, [same seam as parking-lot
  pricing](/lld/parking-lot)).
- **`BalanceSheet`** holds **net balance per user** (positive = is owed,
  negative = owes). Derived from expenses, so it's a cache — rebuildable
  by replay, which is your data-corruption escape hatch.

Each strategy **validates its own args**: exact amounts must sum to the
total; percentages to 100. Validation inside the strategy, not the
manager — the class that defines the rule enforces it
([encapsulation](/lld/oop-and-solid)).

## Money: the two non-negotiables

1. **Never floats.** ₹0.10 isn't representable in binary; pennies leak.
   Integer paise/cents or `BigDecimal`
   ([Level 1 said it](/programming/variables-and-data-types); here it's
   load-bearing).
2. **The conservation invariant: every expense's splits sum exactly to
   the amount, and the global net sums to zero.** ₹100 split three ways is
   33.33 + 33.33 + 33.34 — *someone* gets the spare paisa
   (convention: first participant). Write the assertion; mention it
   unprompted. It's the difference between candidates who've handled money
   and those who haven't:

```python
def equal_split(amount_paise, users):
    base, remainder = divmod(amount_paise, len(users))
    return [Split(u, base + (1 if i < remainder else 0))
            for i, u in enumerate(users)]
    # sum of shares == amount_paise, ALWAYS — assert it in tests
```

## The algorithm: debt simplification

A trip's raw expenses create a messy web of IOUs. But only **net balances**
matter: compute each user's net, then match debtors to creditors:

```
Raw: A→B ₹20, B→C ₹20, C→A ₹10        Nets: A −10, B 0, C +10
Simplified: A pays C ₹10.  (3 transactions → 1; B vanished entirely)
```

<Mermaid chart={`flowchart LR
  subgraph before["Before — raw IOU web (3 transactions)"]
    A1[A] -->|₹20| B1[B]
    B1 -->|₹20| C1[C]
    C1 -->|₹10| A1
  end
  subgraph after["After — net & match (1 transaction)"]
    A2["A (net −10)"] -->|₹10| C2["C (net +10)"]
    B2["B (net 0) — out of the picture"]
  end
  before ==>|"compute nets,<br/>match debtors↔creditors"| after
  style B2 fill:#F3ECDA,stroke:#A6997A,stroke-dasharray: 5 5`} />

The graph view is why this works: individual edges (who-paid-whom) are
noise; only each node's **net flow** matters, and any set of transactions
that produces the same nets is equally valid.

```python
def simplify(net):                       # net: user → paise (sum == 0)
    owes  = [(u, -v) for u, v in net.items() if v < 0]
    owed  = [(u,  v) for u, v in net.items() if v > 0]
    txns, i, j = [], 0, 0
    while i < len(owes) and j < len(owed):
        debtor, debt = owes[i]
        creditor, credit = owed[j]
        pay = min(debt, credit)
        txns.append(Transaction(debtor, creditor, pay))
        owes[i], owed[j] = (debtor, debt - pay), (creditor, credit - pay)
        if owes[i][1] == 0: i += 1
        if owed[j][1] == 0: j += 1
    return txns
```

[Two pointers](/dsa/two-pointers) over debtors and creditors — at most
**n−1 transactions** for n users, each iteration retires at least one side.
Greedily matching *largest debtor to largest creditor*
(two [heaps](/dsa/heaps-and-intervals)) tends to produce fewer
transactions; the true *minimum* transaction count is NP-hard
(subset-sum in disguise) — knowing that "good greedy, optimal is NP-hard,
n−1 is fine" line is exactly the [greedy-judgment](/dsa/greedy) graders
want.

Watch it collapse a five-IOU web into the minimum transfers. First the raw debts
become net balances; then each step pays the biggest debtor's debt to the biggest
creditor, zeroing out at least one person per transfer. Edit the debts to try
your own group.

<Visualizer algo="split-debts" />

## Think it through like the interview

<ThinkThrough
  title="Design Splitwise"
  difficulty="LLD Classic — Strategy + money"
  problem="Users create group expenses — one payer, many participants, split equally / by exact amounts / by percentages. Show who owes whom, and simplify the debts."
  stages={[
    {
      label: "Scope to the three tests",
      prompt: "What three distinct skills is this one prompt secretly probing?",
      reveal: "(1) A Strategy seam — split types vary. (2) Money correctness — rounding and invariants. (3) One real algorithm — debt simplification. Scope to hit all three: 'single currency, ignore settlement execution and auth — OK?' Knowing what the grader is grading is half the interview."
    },
    {
      label: "Spot what varies → Strategy",
      prompt: "Equal, exact, percent splits — and next month, 'by shares'. Where does that variability live?",
      reveal: "Behind SplitStrategy.computeSplits(amount, participants, args) → List<Split>. Each strategy validates its OWN args (exact sums to total, percents to 100) — the class that defines the rule enforces it. New split type = new class, zero edits. Also: Expense is immutable; edits create versions. Finance code never destroys history."
    },
    {
      label: "Handle money like you've been burned",
      prompt: "₹100 split three ways. What does your code return, exactly?",
      reveal: "Integer paise, never floats: divmod(10000, 3) → 3333 r 1 → shares 3334/3333/3333, remainder to the first participant deterministically. Two invariants, asserted: each expense's splits sum exactly to its amount; all nets globally sum to zero (money is conserved). Saying these UNPROMPTED is what separates people who've touched financial code.",
      code: "base, rem = divmod(amount_paise, len(users))\nshares = [base + (1 if i < rem else 0) for i in range(len(users))]\nassert sum(shares) == amount_paise   # always"
    },
    {
      label: "See the graph, then simplify",
      prompt: "A→B ₹20, B→C ₹20, C→A ₹10. What's the minimal settle-up — and what made it easy?",
      reveal: "Only NET balances matter: A −10, B 0, C +10 → A pays C ₹10, done. Algorithm: net out, then two-pointer (or two-heap greedy) match debtors to creditors — at most n−1 transactions. The judgment line: 'true minimum is NP-hard (subset-sum in disguise); greedy is near-optimal; I ship the greedy.'"
    },
    {
      label: "The concurrency follow-up",
      prompt: "Two people add expenses to the same group simultaneously. What races, and what's the elegant fix?",
      reveal: "BalanceSheet update is read-modify-write → lost update. Fix A: atomic per-group mutation (lock/serialized queue). Fix B — the elegant one: make balances DERIVED. Append expenses to a log (appends don't race), compute nets on read or rebuild the cache by replay. That's event sourcing in miniature, and it doubles as your corruption escape hatch."
    }
  ]}
/>

## Walk a scenario + concurrency

"Trip group: A pays ₹300 dinner, equal among A,B,C" →
`EqualSplit.computeSplits` → splits (100,100,100) → BalanceSheet: B −100,
C −100, A +200. "B pays ₹150 taxi, exact: A 50, B 100" → A +50… → nets:
A +150, B −50, C −100 → `simplify` → *C pays A ₹100, B pays A ₹50.* Two
transactions settle the trip.

**Concurrency follow-up:** two expenses added to one group simultaneously
→ the balance update is read-modify-write. Same family as the
[parking-lot race](/lld/parking-lot): atomic per-group mutation (lock or
serialized queue per group), or make balances *derived only* — append
expenses to a log, compute nets on read; appends don't race
(the [event-sourcing instinct](/hld/case-ingestion-pipeline), in
miniature).

<QA q="Why is SplitStrategy the right abstraction, and what would you lose hardcoding split types in the Expense class?">

Split computation is the *varying behavior* — equal, exact, percent,
shares, adjustments all consume (amount, participants, args) and produce
splits, so they slot behind one interface chosen at expense creation.
Hardcoding means a switch inside Expense touched by every new type
(Open/Closed violation), validation rules tangled together, and untestable
combinations. With Strategy, each type validates its own invariant (exact
sums to total; percents to 100) and tests in isolation. Same seam as
pricing in the parking lot — LLD interviews reward recognizing the
recurring shape.

</QA>

<QA q="₹100 split equally three ways — walk through the money handling.">

Store paise integers: 10000 / 3 = 3333 remainder 1 by `divmod`. Shares:
3334, 3333, 3333 — distribute the remainder deterministically (first k
participants get the extra paisa) so results are reproducible and
auditable. The invariant — splits sum exactly to the amount — is asserted
at creation; floats are banned because 0.1 has no exact binary
representation and per-expense rounding drift accumulates into real money.
Across the system, all nets sum to zero (money is conserved); that global
check is your nightly reconciliation test. This tiny question reliably
separates candidates who've touched financial code.

</QA>

<QA q="Is minimizing the number of settlement transactions tractable?">

Finding *the minimum* is NP-hard — it reduces to partitioning users into
zero-sum subsets (subset-sum flavored): each disjoint zero-sum subset of
size k settles internally with k−1 transactions, so minimizing
transactions = maximizing zero-sum partitions. Practical answer: net out
balances, then greedy-match largest debtor against largest creditor with
two heaps — at most n−1 transactions, usually near-optimal, O(n log n).
Saying "I'd ship the greedy and note optimality is NP-hard" demonstrates
both the algorithm and the engineering judgment about when optimal isn't
worth chasing.

</QA>

## Practice — level up

Splitwise's core is a **graph of balances** plus a **greedy** settlement. Train
both halves on these:

<PracticeLadder
  topic="Balances & settlement"
  tiers={[
    {
      label: "Warm-up — net flow on a graph",
      problems: [
        { name: "Find the Town Judge", lc: "find-the-town-judge", difficulty: "Easy", trains: "Net in/out degree on a tiny directed graph — the balance idea." }
      ]
    },
    {
      label: "Core — propagate values across relationships",
      problems: [
        { name: "Evaluate Division", lc: "evaluate-division", difficulty: "Medium", trains: "Walk a graph of ratios — the same shape as balances flowing across IOUs." }
      ]
    },
    {
      label: "Stretch — the literal problem",
      note: "This IS Splitwise's simplify step.",
      problems: [
        { name: "Optimal Account Balancing", lc: "optimal-account-balancing", difficulty: "Hard", trains: "Minimum transfers to settle debts (LeetCode Premium) — greedy ships, optimal is NP-hard." }
      ]
    }
  ]}
/>
