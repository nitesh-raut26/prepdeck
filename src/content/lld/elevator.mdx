---
title: "Design an Elevator System"
summary: "The state-machine classic: elevator states, the SCAN scheduling insight, Strategy for dispatch, and the multi-elevator follow-ups."
order: 12
difficulty: "Classic"
tags: ["LLD", "OOD", "state-pattern", "scheduling"]
---

## Scope it first

"Design an elevator" is two problems wearing one prompt: **object model**
(states, requests, doors) and **scheduling** (which elevator answers, in
what order). Clarify both: *"N elevators, M floors, hall calls (up/down
button on a floor) + cabin calls (floor button inside), optimize average
wait — ignore weight sensors and maintenance mode, OK?"*

The two request types are the first real modeling decision — they carry
different information: a **hall call** is (floor, direction); a **cabin
call** is just (floor). Conflating them breaks scheduling later.

## Entities & relationships

<Mermaid chart={`classDiagram
  class ElevatorSystem {
    +handleHallCall(floor, dir)
    +step()
  }
  class Elevator {
    +id
    +currentFloor
    +direction
    +state
    +addStop(floor)
    +step()
  }
  class Door {
    +open()
    +close()
  }
  class DispatchStrategy {
    <<interface>>
    +pick(elevators, call) Elevator
  }
  class Request {
    +floor
    +direction
  }
  ElevatorSystem "1" --> "*" Elevator
  ElevatorSystem --> DispatchStrategy : delegates
  Elevator --> Door
  ElevatorSystem --> Request : queues`} />

- **`ElevatorSystem`** is the facade: receives calls, delegates "who goes?"
  to a `DispatchStrategy`, ticks the world via `step()`.
- **`Elevator`** owns its position, direction, door and **stop set** — and
  is a textbook **state machine**: `IDLE → MOVING_UP / MOVING_DOWN →
  STOPPED(doors) → …`. Transitions live in one place; "can't move with
  doors open" becomes an invariant, not a scattered `if`.
- **`DispatchStrategy`** (Strategy pattern, like the
  [parking lot's](/lld/parking-lot) spot assignment): nearest-available,
  least-loaded, same-direction-first — swappable without touching the
  elevator.

Here's that state machine drawn out — note what's *missing*: there is no
arrow from MOVING to DOORS_OPEN's neighbor states that skips STOPPED, and
no arrow out of DOORS_OPEN while moving. Illegal transitions simply don't
exist in the diagram, which is the whole point of the State pattern:

<Mermaid chart={`stateDiagram-v2
  [*] --> IDLE
  IDLE --> MOVING_UP : stop added above
  IDLE --> MOVING_DOWN : stop added below
  MOVING_UP --> STOPPED : reached a stop
  MOVING_DOWN --> STOPPED : reached a stop
  STOPPED --> DOORS_OPEN : open doors
  DOORS_OPEN --> STOPPED : dwell elapsed, close doors
  STOPPED --> MOVING_UP : up-stops remain
  STOPPED --> MOVING_DOWN : down-stops remain
  STOPPED --> IDLE : no stops left`} />

Now drive that machine yourself. Step through a call → arrive → doors → idle
cycle, then add an illegal event (an `arrive` while still **Idle**) and watch it
get refused — the invariant "can't arrive when you never moved" is enforced by
the *absence* of a transition, not a scattered `if`.

<Visualizer algo="state-machine" problem="elevator" />

## The scheduling insight: SCAN, not FIFO

Serving requests in arrival order (FIFO) makes the cabin ping-pong: 2 → 9
→ 3 → 8 wastes everyone's time. Real elevators run **SCAN** (the
"elevator algorithm" — disk drives borrowed it *from* elevators): **keep
moving in the current direction, servicing every stop in that direction;
reverse only when none remain.**

The data structure follows from the algorithm: not a queue but **two
sorted sets** per elevator — stops above (served ascending while going up)
and stops below (descending while going down). In code:

```python
class Elevator:
    def __init__(self, id):
        self.id, self.floor, self.direction = id, 0, Direction.IDLE
        self.up_stops = SortedSet()      # or a min-heap
        self.down_stops = SortedSet()    # or a max-heap

    def add_stop(self, floor):
        if floor > self.floor: self.up_stops.add(floor)
        elif floor < self.floor: self.down_stops.add(floor)

    def step(self):                      # one tick of the state machine
        if self.direction == Direction.UP:
            self.floor += 1
            if self.floor in self.up_stops:
                self.up_stops.remove(self.floor)
                self.open_doors()        # state: STOPPED
            if not self.up_stops:        # exhausted this direction
                self.direction = (Direction.DOWN if self.down_stops
                                  else Direction.IDLE)
        # ... mirrored for DOWN
```

A simple, defensible `DispatchStrategy`: prefer elevators **already moving
toward the call in the same direction** (cost = distance), then idle ones
(distance), then opposite-direction ones (distance to their reversal point
+ back). Naming that cost function out loud is the senior move; the exact
formula matters less.

## Where the patterns live

- **State** — the elevator's lifecycle. Guards illegal transitions
  (MOVING → doors open) structurally.
- **Strategy** — dispatch policy, and door-dwell policy if pressed.
- **Observer** — floor displays and hall-button lamps subscribe to
  elevator events rather than being polled.
- **Singleton (resist it)** — one `ElevatorSystem`, but
  [inject it](/lld/oop-and-solid); tests will thank you.

<Callout type="tip" title="Drive the design with `step()`">
Making time explicit — a `step()`/tick method — turns the whole system
into something you can unit-test deterministically ("after 3 ticks,
elevator 2 is at floor 5 with doors open") and demo in the interview
without threads. Real-time is then just a loop calling `step()` on a
timer.
</Callout>

## Think it through like the interview

<ThinkThrough
  title="Design an Elevator System"
  difficulty="LLD Classic"
  problem="Design the classes and scheduling for N elevators serving M floors. Hall calls (up/down on a floor) and cabin calls (floor button inside) arrive continuously; minimize average wait."
  stages={[
    {
      label: "Split the problem",
      prompt: "This prompt hides two different problems. Can I name them before designing?",
      reveal: "Object model (states, doors, requests) AND scheduling (who answers, in what order). Say it out loud — interviewers reward decomposition. Then scope: 'N elevators, M floors, both call types, optimize average wait; ignore weight sensors and maintenance mode, OK?'"
    },
    {
      label: "Find the state machine",
      prompt: "An elevator can't move with doors open. Where should that rule LIVE?",
      reveal: "Not in scattered if-checks — in a state machine. IDLE / MOVING_UP / MOVING_DOWN / STOPPED / DOORS_OPEN with explicit transitions. An illegal move (MOVING + doors open) isn't 'checked for'; it has no transition, so it can't be expressed. Whenever an entity 'behaves differently per mode', reach for State."
    },
    {
      label: "Model the two request types",
      prompt: "Hall call vs cabin call — same class or different? What data does each carry?",
      reveal: "Different. Hall call = (floor, direction) and belongs to the SYSTEM — any elevator may claim it. Cabin call = (floor) and belongs to ONE elevator forever. Conflating them breaks scheduling: dispatch only applies to hall calls. The modeling decision IS the scheduling decision."
    },
    {
      label: "Derive the algorithm, then the data structure",
      prompt: "Requests at floors 2, 9, 3, 8 from floor 1 — what order do I serve them, and what structure holds them?",
      reveal: "FIFO ping-pongs (21 floors). SCAN sweeps up through 2,3,8,9 (14 floors): keep direction, serve every stop en route, reverse when empty. The structure follows: two sorted sets per elevator (stops above, stops below) — not a queue. Algorithm first, data structure second; that ordering is a transferable habit."
    },
    {
      label: "Walk a scenario + break it",
      prompt: "Floor 6 presses DOWN; A is at 3 going up with stops {7,9}; B idle at 8. Who goes — and what's the race?",
      reveal: "Cost A ≈ (9−3)+(9−6) = 9; B is idle, distance 2 → B. The race: two simultaneous hall calls both 'see' B idle and both claim it (check-then-act). Fix: serialize dispatch — single-threaded event loop or a lock around assign-and-add-stop. Buildings emit tens of events/sec; the simple fix is the right fix, and saying THAT is senior judgment."
    }
  ]}
/>

## Walk a scenario + the concurrency question

"User on floor 6 presses DOWN; elevator A is at 3 going up with stops
{7, 9}; B is idle at 8." Dispatch: A is moving away-then-toward (cost ≈
(9−3) + (9−6) = 9), B is idle at distance 2 → **B**. B: IDLE →
MOVING_DOWN, stops {6}; arrives, doors open; user presses 1 → cabin call
adds 1 to down-stops; continues down. SCAN means if someone on 4 presses
DOWN meanwhile, B collects them en route — no extra trip.

**Concurrency follow-up:** hall calls arrive from many floors while
elevators tick. Same answer family as the
[parking lot's last spot](/lld/parking-lot): serialize mutations — a lock
(or single-threaded event loop) around assign-and-add-stop so a call is
assigned exactly once, and make `step()` atomic per elevator. One
elevator's state is touched by one thread at a time.

<QA q="Why is FIFO a bad scheduling policy for an elevator, and what do real systems use?">

FIFO serves requests in arrival order regardless of position, so the cabin
criss-crosses the shaft — request order 2, 9, 3, 8 travels ~21 floors
where SCAN travels ~14 serving everyone. SCAN sweeps each direction
servicing all stops along the way, bounding both total travel and the
worst individual wait (no one is passed twice). It's also why the natural
structure is two direction-sorted stop sets, not a queue — the algorithm
chooses the data structure. Disk schedulers adopted the same algorithm for
head movement, which makes a nice closing line.

</QA>

<QA q="How do you model the difference between hall calls and cabin calls?">

As different request types with different data: a hall call is (floor,
direction) and belongs to the *system* — any suitable elevator may claim
it; a cabin call is (floor) and belongs irrevocably to *one elevator*.
This split drives real behavior: dispatch only applies to hall calls; a
DOWN hall call shouldn't be served by a full cabin sweeping up past it;
and duplicate hall presses across floors dedupe at the system level while
cabin stops dedupe per elevator. Modeling both as bare floor numbers
loses exactly the information scheduling needs.

</QA>

<QA q="Two hall calls arrive simultaneously for the same idle elevator — what guarantees correctness?">

Assignment must be atomic: the check "which elevator is best" and the
mutation "add stop to that elevator" form a check-then-act, so two
concurrent dispatches can both pick the same idle elevator and one
assignment can be lost or doubled. Either funnel all dispatch through a
single-threaded event loop (simplest, and plenty fast — buildings generate
tens of events/sec, not thousands), or lock the dispatch critical section.
Per-elevator `step()` should likewise be atomic so state transitions
serialize. It's the same invariant-at-the-point-of-mutation rule as the
parking lot's last spot and a balance double-spend.

</QA>

## Practice — level up

An elevator controller is a **scheduler**: given the pending requests and the
car's position, pick the next stop — then serve a sweep efficiently. These drills
are that same "choose the next job" decision.

<PracticeLadder
  topic="Scheduling & dispatch"
  tiers={[
    {
      label: "Warm-up — pick the next job",
      problems: [
        { name: "Single-Threaded CPU", lc: "single-threaded-cpu", difficulty: "Medium", trains: "When free, take the best pending task by a policy — exactly 'which floor next'." }
      ]
    },
    {
      label: "Core — assign over time",
      note: "Many requests, limited movers, a clock.",
      problems: [
        { name: "Process Tasks Using Servers", lc: "process-tasks-using-servers", difficulty: "Medium", trains: "Route each request to a free resource as resources free up — multi-car dispatch." },
        { name: "Task Scheduler", lc: "task-scheduler", difficulty: "Medium", trains: "Order work under a cooldown constraint — sequencing a sweep of stops, not just choosing one." }
      ]
    },
    {
      label: "Stretch — concurrent demand on a shared resource",
      problems: [
        { name: "Meeting Rooms II", lc: "meeting-rooms-ii", difficulty: "Medium", trains: "Peak simultaneous demand over a timeline — how many cars a request pattern actually needs." }
      ]
    }
  ]}
/>
