---
title: "Variables & Data Types"
summary: "Named boxes in memory, the types that describe what's inside them, and the bugs that happen when you get types wrong — in Python, Java and C++."
order: 2
difficulty: "Level 1"
tags: ["programming", "variables", "types", "memory"]
---

## What a variable is

A **variable** is a named piece of memory. Recall from
[Level 0](/basics/how-computers-work) that running programs live in RAM —
a variable is you telling the language: *"reserve a spot on the desk, put
this value in it, and let me refer to it by this name."*

```python
# Python
age = 25
name = "Asha"
price = 99.50
is_member = True
```

```java
// Java — the type comes first, and it's mandatory
int age = 25;
String name = "Asha";
double price = 99.50;
boolean isMember = true;
```

```cpp
// C++
int age = 25;
std::string name = "Asha";
double price = 99.50;
bool isMember = true;
```

The label-on-a-box analogy: the **name** (`age`) is the label, the **value**
(`25`) is what's inside, and the **type** (`int`) is the shape of the box —
what kind of thing fits in it.

Variables *vary*: assigning again replaces the contents.

```python
score = 10
score = score + 5   # read current value, add 5, store back → 15
score += 5          # shorthand for the same thing → 20
```

That `x = x + 5` line bothers everyone at first: `=` is not math equality,
it's the **assignment** instruction — "compute the right side, store it in
the left side."

## The core data types

A **data type** describes what a value is and what you can do with it
(you can divide two numbers; you can't divide two names). Every language has
the same basic set, under slightly different names:

| Concept | Python | Java | C++ | Example |
| --- | --- | --- | --- | --- |
| Whole number | `int` | `int` / `long` | `int` / `long long` | `42`, `-7` |
| Decimal number | `float` | `double` | `double` | `3.14`, `-0.5` |
| Text | `str` | `String` | `std::string` | `"hello"` |
| True/false | `bool` | `boolean` | `bool` | `True` / `true` |
| Single character | (1-char `str`) | `char` | `char` | `'A'` |
| Nothing/absent | `None` | `null` | `nullptr` | — |

Two beginner-critical details:

- **`"42"` is not `42`.** Text that looks like a number is still text. `"42" +
  "1"` is `"421"` in Python (text gluing — **concatenation**), while `42 + 1`
  is `43`. Most "weird bug" moments in week one are a string where a number
  was intended — often fresh from user input, which always arrives as text.
- **Decimals are approximate.** `0.1 + 0.2` gives `0.30000000000000004` in
  *every* language, because binary can't represent 0.1 exactly — same as ⅓
  in decimal. Rule: never use floats for money; store paise/cents as
  integers. (Yes, this is an interview question.)

### Contiguous Memory: Arrays (Java & C++)

Before looking at growable lists, we must understand the raw hardware container: the **Array**. An array is a fixed-size row of boxes in memory, all of the same type.

```java
// Java: Declare and allocate an array of size 5
int[] arr = new int[5]; // initialized to [0, 0, 0, 0, 0] by default
arr[0] = 42;            // store at index 0

// Array literal (size is inferred as 3)
int[] primes = {2, 3, 5};

// 2D Arrays (Matrix / Grid - very common in DSA)
int[][] grid = new int[3][4]; // 3 rows, 4 columns
grid[1][2] = 99;
```

```cpp
// C++: Array on the stack (fixed size must be a compile-time constant)
int arr[5] = {0}; // initialize all to 0
arr[0] = 42;

// 2D Arrays
int grid[3][4] = {0};
grid[1][2] = 99;
```
Note: In C++, stack arrays are fixed. For dynamic sizing, we use `std::vector` (covered on the [Collections](/programming/collections) page), which manages a raw heap array for us.

### Converting between types

```python
# Python
age = int("25")        # text → number
label = str(25)        # number → text
print(int(7.9))        # 7 — truncates, doesn't round
```

```java
// Java
int age = Integer.parseInt("25");
String label = String.valueOf(25);
```

```cpp
// C++
int age = std::stoi("25");
std::string label = std::to_string(25);
```

## Static vs dynamic typing, now hands-on

The [previous page](/programming/choosing-a-language) introduced this split;
here's what it feels like in practice:

```python
# Python — dynamic: the TYPE LIVES WITH THE VALUE, names are free
x = 42        # x holds an int
x = "hello"   # ...now a str. Legal. Sometimes a bug you wanted caught.
```

```java
// Java — static: the TYPE LIVES WITH THE NAME, forever
int x = 42;
x = "hello";   // ❌ compile error — caught before the program ever runs
```

Dynamic typing is freedom (fast to write, easy to start); static typing is a
seatbelt (a whole bug category — wrong type reaching distant code — cannot
exist). Industry verdict, worth internalizing early: on large codebases the
seatbelt wins, which is why typed Python and TypeScript took over.

### Python Type Hints in DSA

Python is dynamically typed, but standard DSA templates use **type hints** to document function inputs and outputs. Since you will encounter them in every practice platform (like LeetCode), you must recognize this syntax:

```python
from typing import List, Dict, Optional

class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        # 'nums' must be a list of integers
        # 'target' must be an integer
        # The function returns (->) a list of integers
        return []

    def get_node(self, val: int) -> Optional[ListNode]:
        # Optional[T] means the return value is either a ListNode OR None
        # This is extremely common in linked list and tree problems!
        return None
```
*Note: In modern Python (3.9+), you can write `list[int]` and `dict[str, int]` directly without importing from the `typing` module.*

## Naming: the first skill interviewers actually judge

Code is read far more often than written. Compare:

```python
# bad
d = 86400
x = t / d

# good
SECONDS_PER_DAY = 86400
days_elapsed = elapsed_seconds / SECONDS_PER_DAY
```

Conventions to follow from day one:

- Names describe **contents, not type**: `customer_count`, not `num` or `n2`
- Python/C++: `snake_case` variables; Java: `camelCase`
- Constants in `ALL_CAPS`; booleans read as questions: `is_member`,
  `has_paid`
- One-letter names only for tiny scopes (`i` in a short loop — fine)

<Callout type="tip" title="Interview signal">
In machine-coding rounds (Level 5), reviewers consistently cite naming as a
top differentiator between candidates with identical logic. `inventory` vs
`arr2` is the difference between "reads like a professional" and "reads like
homework."
</Callout>

## What's actually in the box: a first look at references

One more idea, planted now because Levels 2 and 5 depend on it. For simple
types (numbers, booleans), the box holds **the value itself**. For big things
(lists, objects), the box usually holds **a reference** — the *address* of the
data, not the data ("the warehouse shelf number, not the warehouse").

```python
a = [1, 2, 3]
b = a            # copies the ADDRESS, not the list
b.append(4)
print(a)         # [1, 2, 3, 4]  ← a changed too! Same list, two labels.
```

If that surprised you, good — hold the thought. Copying a reference copies
*access to* the thing, not the thing. This single fact explains a huge family
of bugs in every language, and we'll formalize it on the
[Functions](/programming/functions) page (pass-by-value vs pass-by-reference).

## Pointers and References: Memory Addresses Under the Hood

For simple types like `int`, the variable contains the actual value. For complex objects, Python and Java variables contain **references** (pointers to memory addresses under the hood). C++ is unique because it forces you to deal with memory addresses explicitly.

You must master four concepts in C++ pointers and references to write linked lists, trees, and graphs:

### 1. Pointer Variables (`*`)
A pointer is a variable whose value is the **memory address** of another variable. We declare a pointer using the asterisk symbol `*`.
```cpp
int x = 42;
int* ptr = &x; // ptr stores the memory address of x (ptr "points to" x)
```

### 2. Address-Of (`&`) and Dereferencing (`*`)
- **`&` (Address-Of)**: Returns the memory address of a variable.
- **`*` (Dereference)**: Goes to the address stored in a pointer and reads/modifies the value there.
```cpp
std::cout << ptr;  // Prints something like 0x7ffd58bb12c (the address)
std::cout << *ptr; // Prints 42 (goes to that address and reads the value)
*ptr = 99;         // Changes x to 99!
```

### 3. The Arrow Operator (`->`)
If a pointer points to an object/struct, we use the arrow operator `->` to access its members. It is a shorthand for dereferencing followed by member access.
```cpp
struct Node {
    int val;
    Node* next;
};

Node* head = new Node{10, nullptr};
std::cout << head->val;   // Prints 10 (shorthand for (*head).val)
```

### 4. References (`&` in a type)
A reference is an **alias** (another name) for an existing variable. It does not store an address and cannot be repointed.
```cpp
int x = 42;
int& ref = x; // ref is another name for x (any edit to ref edits x)
ref = 99;     // x is now 99!
```

### 5. `nullptr`
`nullptr` represents a pointer that points to nothing (the equivalent of `None` in Python or `null` in Java). In DSA, the end of a linked list or an empty tree node is always signaled by `nullptr`.

#### Summary of Box-and-Address Models:
```
[Value Variable]       [Pointer Variable]
┌───────────┐          ┌───────────┐
│    42     │          │ 0x7ffd... │ ───┐  (Points to x)
└───────────┘          └───────────┘    │
x (at 0x7ffd...)       ptr              ▼
                                     [x's box]
```


## Common beginner mistakes

- **Using a variable before assigning it** — `NameError` (Python) / compile
  error (Java, C++). Boxes must be filled before opening.
- **Comparing with `=` instead of `==`.** `=` stores; `==` asks "equal?".
  C++ will happily compile `if (x = 5)` and assign inside the condition — a
  classic trap.
- **String arithmetic surprises:** `"5" * 3` is `"555"` in Python (string
  repetition), an error in Java. Convert first.
- **Integer division:** `7 / 2` is `3.5` in Python 3 but `3` in Java/C++
  (two ints → int, decimals dropped). Python's `7 // 2` gives the
  truncating version on purpose.
- **Float equality:** `if (a == 0.3)` after float math fails randomly.
  Compare with a tolerance: `abs(a - 0.3) < 1e-9`.

## Think it through

Most week-one "weird bugs" are a type in disguise. Debug the classic one —
text where a number was meant — before revealing each step.

<ThinkThrough
  title="The checkout that says everyone's over budget"
  difficulty="Beginner — find the type bug"
  problem="A user enters two prices and a budget; this code always prints 'over'. Why? p1 = input(); p2 = input(); total = p1 + p2; budget = input(); if total > budget: print('over')."
  stages={[
    {
      label: "Find the type",
      prompt: "What type does input() return, and what does that do to p1 + p2?",
      reveal: "input() ALWAYS returns a string — even '10' is text. So p1 + p2 doesn't add, it CONCATENATES: '10' + '20' becomes '1020'. And `total > budget` then compares two strings lexicographically (character by character), not numerically. The logic is fine; the types are wrong."
    },
    {
      label: "Fix at the boundary",
      prompt: "Where's the right place to turn text into numbers?",
      reveal: "Convert the moment data enters: wrap each input() in float() (or int()). 'Parse at the boundary' keeps the rest of the code working with real numbers, so + adds and > compares magnitudes. This is why user input — which always arrives as text — is the #1 source of type bugs."
    },
    {
      label: "The money subtlety",
      prompt: "Is float the right type for prices and budgets?",
      reveal: "For display, fine; for real money, no — 0.1 + 0.2 gives 0.30000000000000004 because binary can't represent 0.1 exactly. Production code stores money as integer minor units (paise/cents) or a decimal type, never raw floats. Worth saying out loud — it's a frequent interview question."
    },
    {
      label: "Code it",
      prompt: "Convert each input; then the arithmetic and comparison just work.",
      reveal: "The conversions are the entire fix.",
      code: "p1 = float(input('price1: '))     # convert text → number at the boundary\np2 = float(input('price2: '))\nbudget = float(input('budget: '))\ntotal = p1 + p2                    # arithmetic now, not string gluing\nif total > budget:\n    print('over')\n# For real money, store integer paise/cents — floats can't represent 0.1 exactly"
    },
    {
      label: "The rules",
      prompt: "Summarize what prevents this whole bug family.",
      reveal: "Input is text — convert it to the type you need at the edge. Never compare floats with == (use a tolerance). Never use floats for money. And '42' is not 42: the look of a value isn't its type."
    }
  ]}
/>

## Check yourself

<Quiz questions={[
  {
    q: "In Python, input() always returns:",
    options: [
      "A string — even '42' is text; convert with int()/float() before doing math",
      "The correct type, inferred automatically",
      "An int when the text looks like a number",
      "None until you convert it"
    ],
    answer: 0,
    explain: "Because input is always text, '10' + '20' concatenates to '1020' and comparisons are lexicographic. Parse at the boundary: float(input()) or int(input())."
  },
  {
    q: "Why does 0.1 + 0.2 == 0.3 evaluate to False in essentially every language?",
    options: [
      "Floats use finite binary bits; 0.1 and 0.2 have no exact binary form, so the sum is slightly off — compare with a tolerance and use integer cents for money",
      "0.3 is a reserved special value",
      "Python rounds 0.3 down internally",
      "It's a bug specific to the interpreter"
    ],
    answer: 0,
    explain: "Same reason ⅓ can't be written exactly in decimal. Use abs(a - b) < 1e-9 to compare, and integer minor units (or a decimal type) for currency."
  },
  {
    q: "a = [1,2,3]; b = a; b.append(4). What is a afterward, and why?",
    options: [
      "[1,2,3,4] — b = a copies the reference (address), so both names point at the SAME list",
      "[1,2,3] — b is an independent copy",
      "[4] — b replaced a entirely",
      "It raises an error"
    ],
    answer: 0,
    explain: "Assignment copies access to the object, not the object. For an independent copy use b = a.copy() (or list(a)); copy.deepcopy for nested data."
  },
  {
    q: "In Java/C++, `7 / 2` with both operands ints gives:",
    options: [
      "3 — integer division drops the fraction; Python 3 gives 3.5 with / and 3 with //",
      "3.5 in every language",
      "4 — it rounds to nearest",
      "A compile error"
    ],
    answer: 0,
    explain: "Two ints produce an int in Java/C++, truncating toward zero. Python 3's / always gives a float; use // for the truncating version on purpose."
  }
]} />

## Interview perspective

<QA q="Why does 0.1 + 0.2 != 0.3 in every mainstream language, and what do you do about it?">

Floating-point numbers are stored in binary with a fixed number of bits
(IEEE 754). 0.1 and 0.2 have no exact binary representation — like ⅓ in
decimal — so each is stored slightly off, and the error shows after addition:
0.30000000000000004. Handling: compare floats within a tolerance (epsilon),
never with `==`; for money, avoid floats entirely — use integer minor units
(paise/cents) or decimal types (`BigDecimal` in Java, `decimal` in Python).

</QA>

<QA q="What's the difference between value types and reference types? Why does it matter?">

A value-type variable contains the data itself; copying it produces an
independent copy (numbers, booleans). A reference-type variable contains the
address of data living elsewhere; copying it produces a second name for the
*same* data — mutate through either and both see it (lists, objects, in
Python/Java; C++ gives you explicit choice). It matters because aliasing is a
top source of bugs ("function modified my list!"), it determines what
passing arguments does, and shallow-vs-deep copying questions hinge on it.

</QA>

<QA q="Integer overflow — what is it and when did it last matter in the real world?">

Fixed-size integers wrap or overflow past their maximum (~2.1 billion for a
32-bit signed int). Real cases: YouTube's view counter broke on Gangnam Style
crossing 2³¹ views (they moved to 64-bit); the 2038 problem — 32-bit Unix
timestamps overflow in January 2038; binary search's famous `(lo + hi) / 2`
overflow bug lived in the JDK for years (`lo + (hi - lo) / 2` is the fix —
relevant in Level 3). Python sidesteps it with arbitrary-precision ints;
Java/C++ engineers must choose sizes consciously.

</QA>

## Practice

**Beginner**

1. Create variables for your name, age, height in meters and whether you're a
   student; print a sentence using all four. (All three languages if you can.)
2. Predict, then verify: `print("3" + "4")`, `print(3 + 4)`,
   `print(int("3") + 4)` — and the Java/C++ equivalents of each.

**Intermediate**

3. Swap two variables without a third (Python: `a, b = b, a`; how would you
   do it in Java?). Then explain to an imaginary junior why
   `a = b; b = a;` fails.
4. Write the list-aliasing example above, then fix it so `b` is a true
   independent copy (`b = list(a)` / `a.copy()`).

**Advanced**

5. In Python, run `x = 10**100` and print it. Try the same value in Java with
   `long`. What does this tell you about how each language stores integers,
   and what's the cost of Python's approach?

Next: [Control Flow](/programming/control-flow) — making programs decide and
repeat.
