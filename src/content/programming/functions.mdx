---
title: "Functions"
summary: "Parameters, return values, scope, and pass-by-value vs pass-by-reference — how code gets named, reused and reasoned about."
order: 4
difficulty: "Level 1"
tags: ["programming", "functions", "scope", "abstraction"]
---

## What a function is

A **function** is a named, reusable block of code: values go in
(**parameters**), work happens, a value comes out (**return value**).

```python
# Python
def area_of_circle(radius):
    return 3.14159 * radius * radius

print(area_of_circle(2))      # 12.56636
print(area_of_circle(10))     # 314.159
```

```java
// Java
static double areaOfCircle(double radius) {
    return 3.14159 * radius * radius;
}
```

```cpp
// C++
double areaOfCircle(double radius) {
    return 3.14159 * radius * radius;
}
```

Vocabulary, precisely:

- **Define** a function: write its name, parameters and body. Nothing runs yet.
- **Call** (invoke) it: `area_of_circle(2)` — *now* the body runs, with
  `radius = 2`.
- **Parameter** — the variable in the definition (`radius`).
  **Argument** — the actual value passed in the call (`2`).
- **Return value** — what the call turns into. `print(area_of_circle(2))`
  first becomes `print(12.56636)`.

A function call is a *jump*: execution leaps into the body, runs it, and
comes back carrying the return value.

## Why functions exist (it's not just "avoid repetition")

The shallow reason: write the circle formula once, use it everywhere; fix a
bug once, it's fixed everywhere (**DRY** — Don't Repeat Yourself).

The deep reason: **functions let you stop thinking.** Once
`area_of_circle` works, you never again think about π — you think "area,"
one level higher. This is **abstraction**, the same move as Level 0's
[APIs](/basics/what-is-an-api): a name in front of hidden machinery. Software
is towers of functions calling functions; your entire growth as an engineer
is learning to build good towers.

The kitchen analogy: a function is a recipe card. "Make the sauce (see card
12)" — the main recipe stays readable because details live elsewhere.

```python
# This reads like a sentence — each call hides honest work
def process_order(order):
    validate(order)
    charge(order.customer, total_price(order))
    schedule_delivery(order)
    send_receipt(order)
```

## Return early, return often

A function ends the moment it hits `return`. Use that for **guard clauses**
(met in [Control Flow](/programming/control-flow)):

```python
def withdraw(account, amount):
    if amount <= 0:
        return "Invalid amount"          # bail out early
    if amount > account.balance:
        return "Insufficient funds"      # and again
    account.balance -= amount            # happy path, un-nested
    return "Success"
```

A function with no explicit `return` hands back "nothing" (`None` in Python,
`void` functions in Java/C++) — fine for functions called for their *effects*
(printing, saving) rather than their *answer*.

## Scope: where a name exists

**Scope** is the region of code where a variable is visible. Variables
created inside a function are **local** — born when the call starts, gone
when it returns:

```python
def calculate():
    result = 42        # local to calculate()
    return result

calculate()
print(result)          # ❌ NameError — 'result' doesn't exist out here
```

Each *call* gets fresh locals: two simultaneous calls to the same function
don't see each other's variables. That isolation is precisely why functions
are safe to reuse.

Variables defined outside all functions are **global** — visible everywhere.
Convenient, dangerous:

<Callout type="warn" title="Why engineers avoid global variables">
A global can be modified by *any* function, so to debug it you must suspect
*every* function. Ten functions sharing globals aren't ten independent tools —
they're one tangled machine. Pass data in as parameters and out as return
values; keep the plumbing visible. (Code review will enforce this; so do
interviewers in Level 5 design rounds.)
</Callout>

## Pass-by-value vs pass-by-reference

The payoff of the references idea from
[Variables & Data Types](/programming/variables-and-data-types): **what does
a function receive — the thing, or a way to reach the thing?**

```python
def add_bonus(score):          # numbers: reassignment is invisible outside
    score = score + 10

def add_item(cart):            # lists: MUTATION is visible outside
    cart.append("gift")

my_score = 50
add_bonus(my_score)
print(my_score)                # 50 — unchanged

my_cart = ["bread"]
add_item(my_cart)
print(my_cart)                 # ['bread', 'gift'] — changed!
```

The model that explains both: arguments are **copies of what's in the box**.
For `my_score`, the box holds the value `50` → the function gets a copy of
50; reassigning its copy touches nothing outside. For `my_cart`, the box
holds a *reference* (an address) → the function gets a copy of the address —
**pointing at the same list**. Reassigning `cart` inside would be invisible;
*mutating through it* (`append`) changes the one shared list.

- **Python and Java both work exactly this way** (copies of
  references for objects).
- **C++** is the language of explicit choice: `f(int x)` copies the value,
  `f(int& x)` passes a true reference (even reassignment shows outside),
  `f(const std::string& x)` passes a reference but forbids changes — the
  efficient read-only default you'll see everywhere in C++ code.

<QA q="A function modified the list I passed in, but couldn't modify the number I passed in. Explain precisely why." id="pass-by-what">

Arguments are passed as copies of the variable's contents. A number variable
contains the value itself, so the function gets an independent copy —
reassigning it changes only the copy. A list variable contains a *reference*
(the address of the list), so the function's copy points to the **same
list** — mutations through it (`append`, index assignment) are visible to the
caller. But *reassigning* the parameter to a new list would only repoint the
copy, invisible outside. Summary: "pass-by-value, where the value may be a
reference" — true of Python and Java; C++ lets you choose explicitly with `&`.

</QA>

## Writing functions people thank you for

Four rules, learnable now, judged in every interview and code review forever:

1. **One job.** "Calculate the total" — yes. "Calculate the total and email
   the user and update the log" — three functions wearing a trenchcoat.
   (Formally the *Single Responsibility Principle* — Level 5.)
2. **Name = verb phrase that tells the truth.** `send_receipt`,
   `is_valid_email` (booleans read as questions). If an honest name needs
   "and" in it, see rule 1.
3. **Few parameters.** More than three is a smell — bundle related ones into
   an object (`Order`), or your calls become `f(true, false, 7, null, true)`.
4. **Prefer returning over mutating.** `clean(text) → new_text` is easier to
   reason about than silently editing shared data — see the QA above for why.

## Common beginner mistakes

- **Defining but never calling** — then wondering why nothing printed. A
  definition is a recipe card; you still have to cook.
- **Confusing `print` with `return`.** `print` shows a value to a *human*;
  `return` hands it to *code*. A function that prints its answer instead of
  returning it can't be composed: `total(prices) + tax` breaks.
- **Capturing nothing:** calling `s.upper()` in Python and ignoring that it
  *returns* the new string (strings are immutable — it can't edit in place):
  `name = name.upper()`.
- **One mega-function.** A 300-line `main()` works until it doesn't. If you
  can't summarize a function in one sentence, split it.
- **Same name, different things** — shadowing a global or built-in
  (`list = [1,2]` in Python kills the `list()` built-in for the rest of the
  program).

## Think it through

The references model has one infamous trap that bites every Python beginner
exactly once. Predict the output before revealing — getting surprised here is
how it sticks.

<ThinkThrough
  title="The mutable default argument"
  difficulty="Beginner — predict the output"
  problem="What does this print, and why? def add_item(item, basket=[]): basket.append(item); return basket — then print(add_item('a')) followed by print(add_item('b'))."
  stages={[
    {
      label: "Make your prediction",
      prompt: "Write down what you expect the two prints to be.",
      reveal: "The natural guess is ['a'] then ['b'] — a fresh empty basket each call. The actual output is ['a'] then ['a', 'b']. The second call somehow remembers the first. That surprise is the whole lesson."
    },
    {
      label: "Why it happens",
      prompt: "When is the default value [] actually created — every call, or once?",
      reveal: "Default values are evaluated ONCE, when the function is DEFINED — not on each call. So there is a single list object that is the default, created at definition time and reused by every call that omits the argument. Append to it once and that one list keeps growing across calls."
    },
    {
      label: "Connect to references",
      prompt: "Tie it back to the box-and-reference model.",
      reveal: "The parameter basket holds a reference to that one shared default list. Each argument-less call binds basket to the SAME object, so every append mutates the same list the caller sees next time. It's the aliasing idea from earlier, hidden inside a default."
    },
    {
      label: "Fix it",
      prompt: "How do you get a fresh list each call without losing the 'optional argument' feature?",
      reveal: "Use an immutable sentinel (None) as the default, and create the real list inside the body. Now each call that omits basket gets its own new list.",
      code: "def add_item(item, basket=None):\n    if basket is None:        # make a FRESH list each call\n        basket = []\n    basket.append(item)\n    return basket"
    },
    {
      label: "The rule",
      prompt: "State the takeaway in one sentence.",
      reveal: "Never use a mutable object (list, dict, set) as a default argument — it's shared across all calls. Default to None and build the mutable value inside. The same 'evaluated once, shared reference' reasoning explains a dozen other Python gotchas, so the model transfers."
    }
  ]}
/>

## Check yourself

<Quiz questions={[
  {
    q: "A function mutates a list you passed in, but can't change a number you passed in. Why?",
    options: [
      "Arguments are copies of the variable's contents: a number variable holds the value (independent copy), a list variable holds a reference (the copy points to the same list, so mutations show)",
      "Lists are global while numbers are local",
      "Numbers are passed by reference, lists by value",
      "Python decides randomly per call"
    ],
    answer: 0,
    explain: "It's 'pass-by-value, where the value may be a reference' — true of Python and Java. Mutating through the shared reference is visible; reassigning the parameter is not. C++ lets you choose with &."
  },
  {
    q: "What makes a function 'pure'?",
    options: [
      "Its return value depends only on its arguments, and it causes no side effects (mutates nothing outside)",
      "It takes no parameters",
      "It never returns None",
      "It's written in a functional programming language"
    ],
    answer: 0,
    explain: "Pure functions are trivially testable (same in → same out), cacheable, and parallel-safe. Good design concentrates impure I/O at the edges and keeps the core pure."
  },
  {
    q: "print vs return — which statement is correct?",
    options: [
      "print shows a value to a human; return hands it to code, so only a returned value can be composed like total(prices) + tax",
      "They are interchangeable",
      "return prints the value to the console",
      "print can be used as a value inside expressions"
    ],
    answer: 0,
    explain: "A function that prints its answer instead of returning it can't be composed or tested on its result — a classic beginner mix-up."
  },
  {
    q: "Why is `def add_item(item, basket=[])` a classic bug?",
    options: [
      "The default list is created once at definition time and shared across all calls, so it accumulates; default to None and build a fresh list inside",
      "Lists are not allowed as default arguments",
      "It makes the function O(n²)",
      "basket becomes a global variable"
    ],
    answer: 0,
    explain: "Defaults are evaluated once, not per call. A mutable default becomes shared state — the None-sentinel pattern gives each call its own fresh container."
  }
]} />

## Interview perspective

<QA q="What is the call stack, and what happens when function A calls function B?">

The **call stack** is where the runtime tracks active function calls. Each
call pushes a **frame** holding that call's local variables and the return
address (where to resume). When A calls B: A's frame pauses, B's frame is
pushed, B runs; on `return`, B's frame is popped and A resumes exactly where
it left off, receiving B's value. The stack is why locals are isolated per
call, why infinite recursion crashes with a *stack overflow* (frames pile up
past the limit), and how a debugger's stack trace knows the chain of calls
that led to a crash. It becomes load-bearing knowledge for recursion (Level 3).

</QA>

<QA q="What makes a function 'pure', and why do engineers and interviewers care?">

A pure function (1) returns a value determined *only* by its arguments — no
reading globals, clocks, or databases — and (2) has no side effects: it
mutates nothing outside itself. `area_of_circle(r)` is pure;
`send_email(user)` is not. Pure functions are trivially testable (same in →
same out), safe to cache, safe to run in parallel, and composable. Real
programs need impure edges (I/O is the point of software), so good design
concentrates effects at the boundary and keeps the core logic pure — a theme
that returns in Level 7 backends and functional-style React in Level 8.

</QA>

<QA q="Parameter vs argument; define both in one sentence each.">

A **parameter** is the named variable in a function's definition
(`def greet(name)` — `name` is a parameter). An **argument** is the concrete
value supplied at call time (`greet("Asha")` — `"Asha"` is the argument),
which gets bound to the parameter for that call.

</QA>

## Practice

**Beginner**

1. Write `celsius_to_fahrenheit(c)` (formula: `c × 9/5 + 32`), then use it in
   a loop printing a conversion table for 0–40°C in steps of 5.
2. Write `is_even(n)` returning a boolean — then rewrite FizzBuzz from the
   [previous page](/programming/control-flow) using helper functions
   `is_multiple_of(n, k)`.

**Intermediate**

3. Write `largest(numbers)` *without* using built-in `max` — accumulator
   pattern inside a function. Then write `second_largest(numbers)`; the edge
   cases (duplicates, short lists) are the actual exercise.
4. Predict the output, then run:
   ```python
   def mystery(items):
       items.append(99)
       items = [0]
       items.append(1)
       return items

   data = [1, 2]
   result = mystery(data)
   print(data, result)
   ```
   Explain every printed value using the box-and-reference model.

**Advanced**

5. Write `apply_twice(f, x)` that returns `f(f(x))`, and use it with a
   `double(n)` function. Congratulations — functions are values too
   (**higher-order functions**), the gateway to callbacks (Level 8) and
   functional programming.
6. Refactor: take your guess-the-number game from the previous page and split
   it into `read_guess()`, `give_hint(guess, secret)` and `play()`. Notice
   how testable each piece became.

Next: [Recursion](/programming/recursion) — functions that call themselves,
and the call stack earning its keep.
